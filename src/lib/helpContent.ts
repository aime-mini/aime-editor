import type { Locale } from "../i18n";

/**
 * Content of the in-app Help screen (F1).
 *
 * RULE (user requirement): every user-facing feature change MUST update this
 * file — both locales — in the same commit. The Help screen is generated
 * entirely from this list; `keywords` feed the search on top of title + body.
 */
export interface HelpTopic {
  id: string;
  section: string;
  title: string;
  body: string;
  keywords?: string;
}

const EN: HelpTopic[] = [
  {
    id: "open-project",
    section: "Getting started",
    title: "Open a project",
    body: "On the welcome screen: Open Folder picks any folder, Recent reopens the last 8 workspaces, New Project creates a folder and opens it in one step, and Clone Repository takes a git URL, asks where to put it, and opens the clone - no terminal needed. A single quiet line at the bottom reports what Aime found on this machine - it either says everything is ready or names what is missing, and opens the full list (both AI CLIs with their sign-in state, Git, Node, language servers) with the exact command to fix each one. Aime stands in the space to the left of the list, the same greeting she gives on the starting window - on a narrow window there is no room for her and she steps out rather than crowd the buttons.",
    keywords: "workspace recent folder start clone git url github character",
  },
  {
    id: "updates",
    section: "Getting started",
    title: "Updates",
    body: "Aime checks once per launch whether a newer version was published. When there is one, a bar appears at the top of the window with the version number - nothing downloads until you press Update now, and dismissing it keeps quiet until the next launch. Updates are signed, so a build that was not produced by the project's own release job is refused.",
    keywords: "update upgrade version release auto-update signed",
  },
  {
    id: "splash",
    section: "Getting started",
    title: "The starting window",
    body: "Aime greets you while it starts, in a small window of its own, instead of putting an empty editor on screen and filling it in later. Aime herself is on the card: she looks up, waves, glances over at her own name and nods. The greeting stays for about four seconds, and it is not idle time - the project folder is opened and the editor's whole bundle is loaded behind it, so what replaces the greeting is a finished editor rather than a shell that fills itself in. She also says hello out loud, once per launch, in whatever language Aime is set to - switch language in Settings and she greets you in it from the next launch. The voice ships with Aime, so it is the same on every machine rather than whichever voices Windows happens to have installed. Press any key or click to go straight through, and the voice stops with it. If something goes wrong while starting, the greeting steps aside on its own and lets the editor show you what happened.",
    keywords: "splash startup launch start screen logo skip loading character wave animation",
  },
  {
    id: "cli",
    section: "Getting started",
    title: "Open from the terminal",
    body: "Type `aime .` in any terminal to open Aime in the current folder, or `aime <path>` for a specific one. A running app is reused.",
    keywords: "command line launcher code",
  },
  {
    id: "multi-window",
    section: "Getting started",
    title: "Multiple projects at once",
    body: "Ctrl+Shift+N, the window button in the status bar, or New Window on the welcome screen opens an independent window - each has its own project, AI chat, and terminals.",
    keywords: "new window parallel",
  },
  {
    id: "switch-close",
    section: "Workspace",
    title: "Switch or close the workspace",
    body: "Hover the project name on top of the file tree: the folder icon opens another workspace, the X closes it and returns to the welcome screen. Right-clicking the name offers the same actions.",
    keywords: "close folder back exit change project",
  },
  {
    id: "file-tree",
    section: "Workspace",
    title: "File tree",
    body: "Click opens a file; right-click offers Open, Reveal in File Explorer, Git Blame (per-line authors - click a commit to see its patch), New File, New Folder, Rename, and Delete. Drag & drop moves items: drop onto a folder to move inside it, onto another file to move next to it, onto the project name to move to the root.",
    keywords: "drag drop move create rename delete context menu reveal explorer blame",
  },
  {
    id: "live-updates",
    section: "Workspace",
    title: "Live updates",
    body: "Changes made outside the editor - including the AI's edits - appear in the tree and the open file automatically, without collapsing expanded folders.",
    keywords: "watcher refresh sync",
  },
  {
    id: "editor",
    section: "Editor",
    title: "Editing & saving",
    body: "Every file you open gets its own tab, and unsaved text stays put when you switch between them - a half-written thought survives going to another file and back. A dot on the tab marks unsaved changes, the x or a middle-click closes it, Ctrl+W closes the current one. The cloud panel is a tab too, with a cloud icon: opening a file over it puts it behind rather than closing it, clicking its tab brings it back with the file still behind, closing the last file beside it shows it again, and only its own x - on the tab or on the panel - closes it. Ctrl+S saves; undoing back to the saved state clears the dot. A .csv or .tsv opens as a TABLE - header pinned, numbers right-aligned, the delimiter found from the file itself (comma, semicolon, tab or pipe, since a spreadsheet saved in a Vietnamese locale writes semicolons and still calls it .csv), quoted fields with commas and line breaks read correctly, and a search box that reaches every row even when only the first two thousand are drawn; a .md opens as the rendered document - headings, lists, code, tables, links - and a .json or .xml (also .csproj, .plist, .svg and the other XML-by-another-name files) as a folded tree of keys and values, attributes as @name; a file that does not parse says so in the parser's words. The Text button beside the file name shows the raw text for editing, and the choice holds until you open another file.\n\nAuto save is on out of the box: a second after you stop typing, the file is written. That second is deliberate - a write per keystroke would refresh the git status, the tree and the diff view for every character. It matters more here than in an ordinary editor, because the agent reads your project from disk: text that only the window knows about is text the AI cannot see. Switching it off in Settings hands saving back to Ctrl+S, and a file that could not be written (locked, read-only) keeps its dot instead of quietly losing the text.\n\nScrolling deep into a function keeps its heading pinned at the top of the editor, so you can always see what you are inside. That heading comes from the file's outline - the one Monaco brings for TypeScript, JavaScript, HTML, CSS and JSON, or the one its language server answers with - and a file with no outline pins nothing at all rather than guessing from indentation, which is how it used to pin rows of bare braces.",
    keywords:
      "save dirty modified undo ctrl+z monaco tab tabs multiple files close switch sticky scroll outline symbols",
  },
  {
    id: "encoding",
    section: "Editor",
    title: "Files that begin with a byte order mark",
    body: "Visual Studio writes C# files with a UTF-8 byte order mark - 3373 of the 4071 .cs files in the solution this was measured on carry one - and once the file is decoded that mark is a real character sitting in front of line 1. Aime hides it, and saving puts back exactly the mark the file had, so opening a file and saving it is not a change in git. Diffs and commit patches leave it out as well: in a patch the mark sits after the line's + sign, where it used to be drawn as an odd glyph in the middle of the first added line.",
    keywords: "bom byte order mark utf-8 encoding visual studio c# diff patch glyph",
  },
  {
    id: "lsp",
    section: "Editor",
    title: "Code intelligence (completions, types, go to definition)",
    body: "Aime speaks LSP to the same language servers VS Code uses, so typing '.' offers real completions, hovering shows types and docs, F12 jumps to a definition, Shift+F12 lists every reference, F2 renames a symbol across the whole project - files that are not open are rewritten on disk, open ones get normal undoable edits - signature help appears inside call parentheses, and errors underline themselves as you type. Aime highlights ~80 languages out of the box - that part is bundled - and ships full IntelliSense for TypeScript, JavaScript, HTML, CSS and JSON. The rest needs a language server, 20-200 MB each, so none are bundled. Instead the first launch quietly installs the small user-scoped ones for you (Python, PHP, SQL, shell, YAML, Dockerfile) without asking, since that is the deal with using Aime; and opening a file whose language has no server offers that one server right there. It runs the real installer and shows every line. Without any of it you still get syntax highlighting everywhere, and full IntelliSense for TypeScript, JavaScript, HTML, CSS and JSON, which Monaco ships built in. The language chip in the status bar tells you the state: green means a server is running, amber means it is missing and its tooltip carries the exact install command. TypeScript and JavaScript still get Monaco's built-in IntelliSense even with no server installed.\n\nTen languages are meant to simply work after installing Aime: Go, Node, TypeScript, JavaScript, HTML, CSS, Python, Rust, C# and SQL. Four of them need nothing at all, and the servers for Python, SQL, YAML, Dockerfile, PHP and shell install themselves on first launch - they are npm packages that land in your own prefix in seconds. Go and Rust servers now install themselves too, but only when their toolchain is already on the machine: `go install` without Go is a failure dressed as an offer, so that case shows no bare command at all and Let AI set it up handles it instead.\n\nC# is served by the Roslyn language server - the one VS Code's C# extension runs. It has no package to install, so Aime downloads it (65 MB) when you ask, shows every line while it does, and then tells it which project or solution it is looking at, without which it analyses nothing at all. That download is the one server Aime does not fetch behind your back, because 'unattended' has to keep meaning seconds. When Roslyn then asks for the project's NuGet packages, Aime runs dotnet restore for it in the background - the language chip spins while that goes on, and completions fill in the moment it finishes. The first restore of a big solution can take minutes; a restored one is seconds.\n\nJava is served by Eclipse JDT LS, and it works on a plain folder of .java files - no Maven or Gradle needed, since the server builds a project of its own for loose sources. It is a set of jars rather than a program, so Aime runs it on the JVM your machine already has: with a JDK of 17 or newer here, opening a .java file offers the download (45 MB) and the chip turns green a few seconds after it starts - JDT LS boots a JVM and indexes the folder before it answers, which is why that first moment is slower than the other servers. Without a JDK there is no offer at all, because 45 MB of jars cannot complete a single expression without one, and installing a JDK is what Let AI set it up is for. If you have already set this machine up for Java debugging, the download is skipped: the debug adapter runs inside JDT LS, so the copy that is here already serves both.",
    keywords:
      "lsp intellisense completion autocomplete hover definition f12 diagnostics errors gopls pyright rust-analyzer",
  },
  {
    id: "plugins",
    section: "Editor",
    title: "Plugins",
    body: "A plugin is a folder with a plugin.json and a script, in Aime's own data folder - Settings has a button that opens it. It is off until you switch it on, and it runs in a worker of its own: no DOM, no access to Aime's internals, and only the capabilities its manifest asked for (editor, files, ui). A call outside them is refused with a sentence naming what was missing. Its commands appear in the palette like any other, and an edit it makes goes through the editor, so Ctrl+Z takes it back.\n\nThe sandbox is there for ordinary mistakes as much as for bad intentions: a plugin that loops burns one core and leaves the window responsive, one that does not finish starting within five seconds is stopped and told so, one that calls Aime more than a hundred times a second is stopped as a runaway - counted inside the plugin's own worker, so the call that goes past the limit fails there and the flood ends where it started - and its output is capped rather than allowed to grow. Every plugin can also be ended by hand from Settings. Plugins are never loaded from a project, because opening somebody's repository must not run their code.",
    keywords: "plugin extension api sandbox worker capabilities commands",
  },
  {
    id: "ai-requirements",
    section: "AI",
    title: "What the AI needs (and what works without it)",
    body: "The AI runs through an AI CLI installed on your machine: npm install -g @anthropic-ai/claude-code for Claude Code, npm install -g @openai/codex for Codex. Aime probes the selected CLI on start: if it is missing it shows the install command, if it is not signed in it offers a one-click sign-in. Without AI the editor, file tree, terminal and Git keep working normally.",
    keywords: "install login sign in requirements offline manual no ai codex claude",
  },
  {
    id: "ai-provider",
    section: "AI",
    title: "Choose the AI CLI & sign in",
    body: "The first chip in the AI panel header switches between Claude Code and Codex. Switching starts a new session, because a resume id belongs to one CLI - previous sessions keep the CLI they were created with. The key button next to it signs you in: it opens a terminal tab running that CLI's own login command (claude auth login / codex login), so your credentials stay with the CLI and never pass through Aime. It turns green once you are signed in, and clicking it then just confirms that; after signing in, Aime detects it by itself.\n\nAn API key works instead of signing in, for providers that take one. Paste it under Settings > AI > API key (the sign-in notice links there too): it is stored on this machine only, handed to Aime's own runs as an environment variable (ANTHROPIC_API_KEY for Claude Code), and never shown back once saved - remove and re-paste to change it. While a key is saved it takes precedence over that CLI's login for everything Aime runs, though your own terminals are untouched. Codex takes a key through the same field but by a different route, and the field says so: its CLI keeps its own keys, so Aime hands the key to codex login --with-api-key and keeps no copy - which replaces the account Codex was signed in with, so it asks you to confirm first, and only codex login can put that back. After a key is accepted Aime asks the CLI what it is now using and tells you the answer: confirmed, or not using a key at all (which is what a mistyped variable name in providers.json looks like), or nothing - some CLIs report neither, and Aime says so rather than pretend. No CLI checks whether a key is valid at the moment you paste it, so a wrong key still shows up as a failed answer.",
    keywords: "provider switch codex claude login sign in key account api key anthropic token",
  },
  {
    id: "ai-add-provider",
    section: "AI",
    title: "Use another AI CLI",
    body: "Claude Code and Codex are built in, but any command-line AI can be added without waiting for a new Aime release: 'Add an AI CLI' in the command palette creates providers.json with a working example and opens it. Describe the command, where the prompt goes ({prompt}), how to resume a conversation ({sessionId}), and whether it prints plain text or JSON per line. The new CLI then appears in the provider picker like the built-in ones. Aime cannot know its models or its permission flags, so it sends what every CLI understands: the instruction in the prompt itself - including your AGENTS.md when the CLI has no memory file of its own. If your CLI can read its prompt from standard input, set promptStdin to true and drop {prompt} from the arguments. On Windows this is not a preference: a CLI installed by npm is a .cmd shim, only cmd.exe can run one, and cmd.exe ends a command line at the first line break - measured, the shim then receives an *empty* argument list and exits 0, so the CLI answers a prompt it never got and the run looks successful. Aime refuses that combination outright and says which setting fixes it, rather than letting a model answer from nothing. A CLI that is a real executable is spawned directly, arguments untouched, so it has no such limit. The built-in providers use stdin regardless. You do not have to write any of that yourself: Settings > AI > Add an AI CLI takes the name of a CLI and hands the job to your agent, which reads that CLI's own help, runs it once to see what it prints, and writes the entry for you - it works in its own progress window, not in your conversation. Either way Aime watches providers.json and re-reads it the moment it changes, so a new CLI appears in the picker without restarting the editor; if the file ever stops being valid JSON, Aime keeps the built-in providers and ignores only the configured ones.",
    keywords: "providers.json generic adapter gemini custom cli add provider reload restart",
  },
  {
    id: "ai-chat",
    section: "AI",
    title: "Chat with the AI",
    body: "With a folder open, type a prompt and press Enter. The AI reads and edits your project and runs commands; tool calls appear as chips, cost and duration after each turn. Every turn also carries what you are looking at, so 'what does this do?' or 'give me the connection details of this one' needs no pointing: the file in front (as an @ mention the CLI reads), the cursor line and any selected text, the other open tabs, and whichever view holds the editor area - a diff, a commit, a work item, a task run, or the cloud panel down to the cloud, the account, the open resource and the configuration already read for it. Reads that can carry a credential are never included, revealed on screen or not; and this block is attached to what is sent, never shown as your words in the transcript. The answer is rendered as the Markdown the AI writes - headings, lists, code blocks, quotes, links, bold and italics, and pipe tables with their column alignment - while what you typed stays exactly as typed; nothing in an answer can become markup of its own, a script tag stays the words of a script tag. The answer is typed onto the screen at the pace it arrives: measured against the real CLI, text reaches Aime in chunks of a couple of lines every three-quarters of a second, so each chunk is spread over the time the next one takes rather than landing as a jump - and anything that is not text, a tool chip or the end of the turn, first shows every character still waiting, so nothing is ever out of order. While a turn is running the panel says which kind of waiting it is: three dots and a clock counting seconds until the first word arrives, a blinking caret behind text that is still coming, and a spinner on the tool chip while its command runs - when the turn ends, all three go. Stop cancels a running turn. Shift+Enter adds a line, and the divider above the box drags it taller when the prompt is a paragraph rather than a sentence - where you leave it is remembered.",
    keywords:
      "prompt send claude agent tool resize taller height drag waiting thinking dots caret spinner streaming",
  },
  {
    id: "ai-model",
    section: "AI",
    title: "Model & effort",
    body: "The chips above the chat choose the model and the reasoning effort of the selected CLI. For Claude, aliases like Opus (latest) track the newest release while pinned versions (e.g. Opus 4.8) stay fixed; for Codex the list is its model catalog (GPT-5.6 Sol, Terra, Luna…). The effort list follows the chosen model - only levels that model accepts are offered. Auto leaves the decision to the CLI's own settings.",
    keywords: "opus sonnet haiku fable gpt codex version xhigh ultra effort picker",
  },
  {
    id: "ai-approve",
    section: "AI",
    title: "How much the AI may do (permissions)",
    body: "The shield chip cycles three levels: FULL - every tool runs unprompted; EDITS - files are edited freely while commands stay sandboxed to this project; READ-ONLY - the AI reads and explains but changes nothing, which is the safe way to explore an unfamiliar codebase. The choice is remembered and applies to both CLIs, mapped onto each one's own flags. Aime deliberately has no per-command Allow/Deny popup: that needs a bundled Node sidecar only one CLI supports, and it would cost the app its size and speed.",
    keywords: "permission bypass sandbox read-only plan command safety shield",
  },
  {
    id: "ai-usage",
    section: "AI",
    title: "Usage & cost",
    body: "The activity button above the chat shows this session's totals: input/output tokens and cache reads/writes, plus cost for CLIs that report one. Claude Code reports a price per turn; Codex bills through your subscription and reports none, so Aime shows tokens only instead of inventing a number.",
    keywords: "tokens cost money spend activity subscription",
  },
  {
    id: "ai-undo",
    section: "AI",
    title: "Taking a turn back",
    body: "Letting an AI change your project is only comfortable if changing it back is one click. Under every answer that touched files, Aime says how many it changed and offers Undo this turn, which puts each of them back exactly as it was before you sent the message - including files the AI created, which are removed. Anything you had staged yourself stays staged, and your own edits from before the turn are kept. This needs the project to be a git repository; without one, no undo is offered rather than a promise that cannot be kept.",
    keywords: "undo revert rollback checkpoint mistake ai changed files safety",
  },
  {
    id: "ai-sessions",
    section: "AI",
    title: "Sessions & memory",
    body: "Chats are saved per project and restored when you reopen it - the AI resumes with its full context, even after a restart. The CLI you picked is part of that: reopening a project continues its newest conversation with that CLI, and if there is none it starts a fresh one rather than switching CLI behind your back - a conversation cannot change CLI half way, because the resume id belongs to the one that made it. The history button switches between the project's last 20 sessions, and opening an older one brings its CLI back with it; + starts a fresh one. The AI also maintains .aime/PROGRESS.md as a progress journal, so long tasks survive any interruption. That folder is Aime's notes about your project, not part of it, so it ignores itself: the first time it appears Aime writes .aime/.gitignore, and git stops seeing the folder without your own .gitignore being touched. Edit that file and it is yours - Aime never rewrites it.",
    keywords: "resume history restore context progress journal remember gitignore commit",
  },
  {
    id: "ai-memory-editor",
    section: "AI",
    title: "Edit the AI's memory",
    body: "The brain button above the chat (or 'Edit AI memory' in the command palette) opens the memory editor. The Project tab edits one canonical AGENTS.md in your repo (conventions, architecture - shared via git): Codex reads it natively and Aime keeps an @AGENTS.md import in CLAUDE.md so Claude reads the very same file - no copies to drift apart. The Global tab edits the selected CLI's own user-level file (~/.claude/CLAUDE.md or ~/.codex/AGENTS.md). The AI re-reads them every turn, so saves take effect immediately.",
    keywords: "memory agents.md claude.md global project knowledge notes brain",
  },
  {
    id: "cloud",
    section: "AI",
    title: "Connect a cloud, and let the AI know what is already there",
    body: "The cloud button in the status bar opens a console of its own, laid out the way every cloud console is: the clouds as tabs across the top - Azure, AWS, Google Cloud, Supabase - the ACCOUNTS that cloud's CLI holds down the left, and the selected account filling the rest. The first time it opens it asks which of the four clouds you actually use, because a tab for somebody else's cloud is a tab in the way: tick the ones you want and the answer is remembered for this machine. Every tab has its own x - middle-click works too - and the plus at the end of the strip brings any cloud back; closing a tab closes only the tab, so a cloud that returns is still signed in with everything already read from it still in hand, and closing the last one asks the question again. Every account is listed, not one: measured on the machine this was built on, `az` held four subscriptions across two signed-in users and two tenants, and `aws` held 28 profiles, so the account list is a real navigation surface with its own filter box. Azure's and AWS's account lists come from what the CLI keeps on disk, so opening those tabs costs no cloud calls at all; Google Cloud's account is a PROJECT, listed under the Google account that can see it, and since `gcloud` keeps no project list on disk that tab makes one `gcloud projects list` per signed-in account when it opens, then keeps the answer. Selecting an account is the ONE call that reaches the network - one generic command per cloud, `az resource list`, `aws resourcegroupstaggingapi get-resources` or `gcloud asset search-all-resources` (Supabase's three listings for Supabase), shown on screen while it runs - and the answer is kept, so coming back to an account costs nothing. It brings back up to ten thousand resources with their tags; the first real account measured held 6,252. An account opens as its RESOURCES - the grouped list of what is in it - because that is what a console is opened for; the Applications view beside it answers the other question, 'which of these ten apps is this', and is one click away. In that view the panel chooses, from the data, the tag key that best divides the account into applications - not a fixed list of favourite keys, which is how a team's `Area` tag went unused - and says which key it used in a dropdown where you can pick any other candidate, the CloudFormation stack the cloud recorded on each resource, or the resource group. The applications are listed down a rail, biggest first, each with a bar showing what it is made of; beside them the account answers what the rail cannot - how much of it carries no tag saying which application it is (one click opens exactly those resources), which services it actually runs on, as a bar per type, and which regions they are in. Open one application and it is drawn as a diagram: three lanes, top to bottom, where requests come in, where the code runs, where the data lives, with what surrounds it - logs, identity, registries - underneath. All three lanes are always drawn, so an empty one is a fact too: a lane saying 'nothing here' is how a back-end that runs on a trigger, with no endpoint and no database of its own, says so in one look. Every KIND of resource is one box carrying its count, with the instances inside it, and the arrows are that tier flow rather than measured dependencies - a real dependency lives in each service's own configuration, which the resource detail reads. The Resources view is the same account as a grouped table - by type, group or region, groups folded so six thousand rows open as forty headings with counts, a chip strip to narrow to one kind, collapse all and expand all - and the search box searches everything, name, type, group, region and tag values alike. Click any resource and it opens over the top: Overview, Connect, Deployed alongside, Raw. The configuration in it comes from the CLI itself through reads the AI PLANS ONCE PER KIND of resource - Aime asks how a Lambda function, an App Service or a Cloud Run service is read, giving the AI the shape of the identifier with the account and the name taken out and, for AWS, the CLI's own catalogue of that service's commands read from the models beside its binary - and then Aime CHECKS every proposed command before keeping it: for AWS the operation and every flag must exist in the CLI's own service model, for Azure and Google Cloud the CLI's own `--help` must accept the command (measured: exit 0 for a real command, 2 for a misspelled one); it must be a read (describe, get, list, show), it must carry every flag the CLI's own synopsis lists as required - reported 2026-09-10, `gcloud logging buckets describe` was kept without `--location` and answered *argument --location: Must be specified* for every bucket of that kind, because the command existing is all an exit code proves - it may not carry the flags Aime adds itself, and no value may hold anything a shell could misread. Then Aime RUNS it, once, against the resource you opened - because a command existing is not a command working, and a plan is kept per KIND, so a command that cannot address the resource would be that kind's answer on this machine for good: measured across a real Google account on 2026-09-11, `gcloud iam service-accounts describe` had been given the relative name where it takes the account's email and answered a 404 page for all fifteen service accounts, and `gcloud logging buckets describe` the same for all twenty-six log buckets. A read that fails goes back to the AI with the CLI's own words - the resource's own parts swapped back out for the placeholders first, so the model still never sees one of your resources - and is asked again, twice at most; what still does not run is dropped rather than stored, with the CLI's sentence beside it. A refusal that is not the command's fault is told apart from one that is: a project with the API switched off, no billing or a lapsed sign-in refuses the right command in exactly the same breath, so that read is kept, the CLI's words are shown where its answer would be - with the button that enables that API, the same one the listing offers - and the plan is written down as unproved so the next open tries it again. A read that can carry a credential is never run to prove anything: it waits for your click, as it always has. What survives is written to disk, so the first resource of a kind waits a few seconds for the plan and every later one does not, and the plan is per machine, not per project. The name a command is given is the resource's own, never the label on screen - measured, a quarter of that account's resources answer a display name like `Default compute service account` where the command wants an email or an id. Azure always starts from `az resource show`, which answers for every type. Google Cloud is TWO CLIs rather than one: `gcloud` has no `bigquery` commands at all, so a BigQuery dataset is read with `bq` - BigQuery's own CLI, which ships in the same folder of the Cloud SDK and so needs no install of its own - and the AI names it on the read. `bq` may be used for BigQuery and for nothing else, and no third CLI may be used at all. Some kinds no CLI reads: measured over a real Google account, seven of its twenty-one kinds have no `gcloud` command - Firebase apps and projects, security rulesets and releases, identity providers, service account keys - and three of those belong to services `gcloud` has no command group for. Aime reads them itself rather than asking you to install anything: Cloud Asset Inventory, the same API that listed the account, hands back a resource's whole body when asked for one, so those kinds now open with their real configuration - a Firebase app's package name and app id, a ruleset's own source, a key's algorithm and validity. It is added only where the AI could not give a read, it is tried for real like any other, and the search that finds the resource is unwrapped so what you see is the resource and not the search around it. A plan file written before connection facts existed is read as a kind nobody has asked about yet and planned again the first time one of its resources is opened: eight of the sixteen plans on the machine this was built on were in that shape, carrying commands and no facts whatever, and one of them was empty - it could show nothing and could never have been asked again. Each read is shown as the exact command a developer can copy and run themselves, with its answer as a table of properties rather than a blob, nested sections folded, every value one click to copy; the Connect tab pulls out whatever the payload calls an endpoint, host, port or URL and puts it first. Above those it shows what needs no call at all: measured across a real Google account, for a large share of what people have - Pub/Sub topics, buckets, BigQuery datasets, App Engine apps - the connection detail IS the resource's own identity written the way an SDK takes it, and Aime already holds every part of it from the listing. So the AI is asked, once per kind, for those too, as templates like `projects/<project>/topics/<name>`; Aime fills them in per resource and puts them under 'Paste these into your app', each one click to copy, with no network call and no waiting. A template with no placeholder in it is refused, because it would say the same thing for every resource of the kind, and a credential can never be one of these - a key cannot be worked out from a name, so those stay reads behind Reveal. Anything that can carry a credential - keys, connection strings, publishing credentials - is marked as such by Aime whatever the AI called it, never runs until you click Reveal on that one read, and its answer stays on the screen: never written to a file, never handed to the AI, never put in a prompt. Signing in: an Azure account whose token has expired shows the CLI's own words - measured, `az account list` answers from cache while `az resource list` fails with `Status_InteractionRequired` - and a Sign in button that runs `az login --use-device-code` for that account's tenant. The device-code flow is what keeps the browser in FRONT of the editor: opened by the CLI itself, several processes down from Aime, the sign-in page landed behind the window; now the CLI prints a page and a code, Aime opens the page from its own process, shows the code large and puts it on your clipboard, and reloads the account when the CLI reports success. Aime never sees a credential - only that one-time public code. Google Cloud runs the same idea the other way round: `gcloud auth login --no-launch-browser` prints its consent page instead of opening it, Aime opens the page in front, and when the page shows a verification code you paste it into the box in the panel - it goes to the CLI's stdin and nowhere else; because the code is shown by the page rather than sent back to this machine, the link can be copied and opened on a phone or another computer when a policy blocks Google sign-in in the browser here, and the panel reloads the project when the CLI reports success. An AWS profile is signed into the way its own config says: `aws configure` for the profile that actually holds the keys (a role profile points at its source profile), `aws sso login` for an SSO profile - and those run in a terminal tab, because keys typed at a prompt belong in a real shell Aime never reads. Whichever way you sign in - the panel's button, a terminal tab, or a shell of your own - the panel notices on its own: every sign-in ends with the CLI rewriting its own files (`~/.azure/azureProfile.json` and its token cache, `~/.aws/credentials` and `config`, the tokens under `~/.aws/sso/cache`, `gcloud`'s `credentials.db` and its configurations under `%APPDATA%\\gcloud` on Windows and `~/.config/gcloud` elsewhere), Aime watches those on Windows, macOS and Linux alike, and when one changes for a cloud that was showing the signed-out answer it probes that cloud again, re-reads its accounts and retries every listing the CLI had refused - so the red dot turns green and the table fills without a click on Try again. Accounts that were already listed are left alone: a token quietly refreshed in the middle of a listing rewrites the same file, and that is no reason to call the cloud again. Switching accounts in the panel changes nothing outside Aime: every read passes the subscription, the profile, the project and the region explicitly, and rewriting the default your own terminal uses is a separate, deliberate action - the star beside an Azure subscription or a Google Cloud project (`az account set`, `gcloud config set project`; AWS has no such default). A cloud with no CLI offers to install it and probes it again the moment the install finishes (winget on Windows, Homebrew on macOS and on a Linux box that has brew, the vendor's page where neither carries it; Supabase's release binary is downloaded directly since no Windows package exists), A listing that fails is read rather than shrugged at: the CLI's own words are shown, and the action offered is the one that fits them - a Sign in button only when the CLI is complaining about who you are, and when Google Cloud refuses because an API is not enabled, a button that enables that API in a terminal tab with the exact `gcloud services enable` command on it, beside a link to the page Google itself named. The API and the project both come out of the CLI's message; Aime does not decide which API you need. That refusal used to arrive for every project at once, and the reason is worth knowing: Cloud Asset checks whether its API is on in the project the call is BILLED to, which by default is whatever `gcloud config set project` last pointed at - not the project you are looking at - so one default set in your own terminal turned every tab in the panel red. Aime's listing is billed the old way instead, with `--billing-project LEGACY`, the CLI's own value for sending no billing project at all; measured across thirteen projects, so what the panel can show you no longer depends on a setting your terminal owns. Making an account the CLI's own default (the star in the rail) asks first, because it writes to the CLI's config and changes what your own terminal does. Supabase works the same way with its own shape: an account is a PROJECT under the organization that owns it, listed from `supabase projects list`, and a project is its Postgres database, its Edge Functions and its preview branches - the three things a project is made of, since its CLI has no inventory command; a read is checked against the CLI's own command tree rather than `--help`, because that CLI answers `--help` with exit 0 even for a command that does not exist. Its sign-in runs `supabase login` in a terminal tab - the CLI refuses its browser flow outside a real terminal - and since it keeps the token in the OS keyring rather than in a file, the panel asks the CLI every few seconds for three minutes whether the sign-in happened, then reads the projects on its own. Finally the radar - 'Write to AI memory' on the account header - asks the AI to find out what is ALREADY RUNNING in that account, read-only, and writes what comes back into your project's AGENTS.md, the file every AI CLI reads on every turn, so the next deployment or bug hunt starts from your real web apps, databases and queues rather than from what a project of your kind usually has; every discovery also has to say what it could NOT see, replaces its own section each time, never touches your own notes, and writes nothing at all if the answer comes back unreadable.\n\nWhat you can do to this - the third tab of any Google Cloud resource - is the panel doing the work rather than describing it. The AI is asked, once per KIND of resource and never again, what a developer actually does to one of those: tail its logs, restart it, change an environment variable, move traffic, scale it. It answers commands written against placeholders and never sees a real resource, and every one of them goes through the same checker a deployment's steps go through - the words must exist in `gcloud --help` on this machine, nothing may touch the account, and no flag may replace a running service's settings. Deleting IS offered here, unlike in a deployment, because removing a resource is ordinary work and refusing it only sends you to the web console to do the same thing with less ceremony: Aime reads the command itself to know it removes something - never the AI's word for it - shows it in full, and the button stays dead until you have typed the resource's own name - so what is offered is what Aime could prove, and the count of what it refused is on the pane. A read runs on the click. A write shows the command first, filled in with the resource's name and region and pinned to the project and account Aime adds, with its values editable (that `KEY=VALUE` on an env var is yours to type) and checked again after the edit; only Run it starts anything. Output arrives line by line where you are, with a Stop, and the exit code at the end.\n\nDeploy - the button on a Google Cloud project's header - deploys the open project there, and it is built so that the AI never holds the cloud CLI: measured, Claude Code's plan mode still runs commands, so instead the AI reads your repository with files-only tools and answers PLANS, Aime checks and runs them. Four steps. First the AI reads the repository and the project's inventory and says what the app is, which resources already are it, and which reads it wants (a `describe` of the existing service); Aime runs those reads. Then it writes the plan: the target (an existing service, or a proposal with architecture, cost and performance when nothing exists yet), the settings it promises to keep, every `gcloud` command, the files it wants written, and how the result is proved - the read that answers the URL and the status a GET on the health path must return. Aime checks every command before you see it: the words must exist (`gcloud … --help` exits 0), nothing may delete, remove or touch the account - with one measured exception, `gcloud projects add-iam-policy-binding`, because a build from source fails on a new project until its build service account is granted a role, and refusing that would send you to the web console for the commonest failure there is; that grant may only ADD a binding, only to a `serviceAccount:`, and never `roles/owner`, `roles/editor` or an IAM-admin role, and for a service that already runs no `--set-*`, `--clear-*` or `--remove-*` flag is allowed because those replace or wipe what the service has - `--update-*` merges. Nothing has run when the confirm page appears; it shows every command exactly as it will run, with the project and account pinned by Aime, and Deploy is the only way on. Then Aime writes the files through the AI, reads the kept settings, runs the steps one by one with their output in the log, hands a failing step back to the AI with its output for a rewritten remainder (six rounds at most, and the same failure twice stops it), reads the kept settings again and reports any that changed, and finally asks the deployed URL itself over HTTP - Done is a service that answered. When that request comes back with the wrong status the service's own root is asked as well, because the two look identical from one request and are not the same thing at all: measured on a real deploy, Cloud Run answered `/healthz` itself with Google's 404 page and never passed it to the container while every other path served, so a deployment that had worked would otherwise have been reported as one that failed. Stop ends whatever is running; Back returns to the resources while the deploy carries on.",
    keywords:
      "cloud azure aws gcp google supabase connect sign in login cli account subscription discover inventory architecture infrastructure deploy devops memory agents.md read-only secrets tab choose pick hide close quota billing default project cloud asset api not enabled bigquery bq dataset",
  },
  {
    id: "mcp",
    section: "AI",
    title: "MCP servers (the AI's plugins)",
    body: "The plug button in the AI panel header (or 'MCP servers' in the command palette) manages the Model Context Protocol servers of the selected CLI - issue trackers, docs, databases the AI can then use as tools. 'Browse & search servers' swaps the dialog to a curated list grouped by what they do - issue boards, design tools, docs, cloud platforms, local tools - and a search box over the official MCP registry, so anything published there is one click away. Picking an entry fills the form (including the environment variables it requires) instead of adding it behind your back; you can also paste a URL or a command such as npx -y server-github yourself. Aime drives the CLI's own mcp commands, so servers stay where the CLI expects them and its health checks and OAuth keep working - note that Claude Code stores them per project while Codex stores them globally; the key button on a row signs in to that server in a terminal.",
    keywords: "mcp plugin tools server add remove connector github notion",
  },
  {
    id: "git",
    section: "Git",
    title: "Git panel",
    body: "The Git tab in the sidebar (or the branch name in the status bar) shows your changes: stage/unstage per file or all at once, discard changes, commit (Ctrl+Enter in the message box), push and pull. Click a file to see its diff against HEAD. That diff opens on the first change instead of at line 1, follows the file as you keep editing it, and shows a change that only moved whitespace - which a diff editor hides unless told not to. When the two sides really are identical it says so, rather than leaving you to hunt for a change that is not there: what git listed is then a staged change, a line-ending change, or a file mode change. The Git tab itself carries a badge with the number of uncommitted files, turning red when one of them is conflicted, so you can tell whether there is anything waiting without leaving the tab you are on. The History section (collapsible, own scroll, Load more) lists commits - click one to open it as a list of the files it touched, and click a file for that file's patch. A renamed file is asked for under both of its names, so it reads as the rename it was rather than as a whole file added; a merge is read against the branch you merged into, which is the only way git will show what it brought in; and a file git reports no line-by-line change for says so instead of opening an empty editor. Changes, History and Stash are three sections with a divider each: drag one and the others give way, fold one and it costs a single line. New files are listed one by one even when a whole folder is new, so nothing hides behind a folder row. Changed files get letters in the file tree, files git ignores are greyed out there (an ignored folder greys in one go, without its contents ever being listed), and changed lines get colored marks in the editor gutter. Every operation says it is happening: fetch, pull and push show a spinner on the button you clicked, and anything started from a menu that has closed - merge, checkout, stash, tags, revert, reset - names itself on a line under the branch bar. Before this they were silent for the seconds they take against a remote, which reads as an editor that is not responding rather than one that is working. Smart by default: committing with nothing staged stages everything first, and the first push of a new branch sets its upstream automatically. Works entirely without AI; with AI, the sparkle button writes the commit message from what the next commit will actually contain: the index when something is staged, and otherwise your working changes - files git has never seen included, since a `git diff` shows none of them and a change made entirely of new files is exactly the one worth describing.",
    keywords: "commit stage diff push pull branch source control version init",
  },
  {
    id: "git-review",
    section: "Git",
    title: "A second opinion before you commit",
    body: "Review, next to the commit button, sends what you are about to commit to the AI and lists what it noticed: problems marked with a red !, smaller notes with a dot, each one clickable to open the file it is about. It is an opinion and never a gate - nothing is blocked, and dismissing it costs one click. With nothing staged it reviews your working changes instead, since that is what you are about to stage - new files included, which no `git diff` reports.",
    keywords: "review ai second opinion before commit findings issues suggestions",
  },
  {
    id: "git-branches",
    section: "Git",
    title: "Branches, remotes and tags",
    body: "Click the branch name to switch branches, create one, rename the current one, merge another into it, or delete one - deleting a branch that still holds unmerged commits asks a second time before losing that work. The list holds every branch, not only the local ones: below the local branches sit the remote branches you have not checked out yet - the whole of a fresh clone's team work, marked with a cloud - and picking one checks it out as a local branch that tracks it. A remote branch you already have locally is listed once, under its local name. Rename, merge and delete act on local branches only. The actions sit at the top of the menu and the branches below them, so a repository with a thousand branches does not bury them. From a dozen branches on the menu opens with a filter box: type any part of a name and the list narrows to what matches, and Enter takes the best match - and a list longer than the window scrolls inside the menu instead of running off the screen. Only the first couple of hundred entries are drawn, with a line under them saying how many more there are: those are reached by typing, which searches the whole list, not only what is on screen. The ... button next to Refresh manages remotes (add one, or repoint an existing one at a new URL) and tags (create lightweight or annotated, delete, push them all). The download arrow fetches remote branches without touching your files - fetch first when a branch a teammate just pushed is not in the list yet.",
    keywords:
      "branch rename delete merge remote origin url tag fetch prune checkout tracking clone all branches filter search long list scroll more entries",
  },
  {
    id: "git-commit-view",
    section: "Git",
    title: "Reading a commit",
    body: "Clicking a commit in History opens it in the editor area, and what you get is the commit as a person reads one: its subject, who wrote it and when, the body of its message, and then the files it touched as a list - each with the letter for what happened to it (added, modified, deleted, renamed) and how many lines moved. Clicking a file shows that file's patch, and only that one. It used to be a single `git show` poured into one editor: a stat block nothing could be clicked in, followed by every file's patch run together, which on a commit touching twenty files is a wall of text you scroll through looking for the one file you came for. A rename is listed under the name it has now, since that is the one you want to open, and hovering it shows where it came from. Binary files say so rather than showing a patch that means nothing. Each patch is fetched only when you open it, so a commit touching a hundred files still opens at once.",
    keywords:
      "commit history file list changed files patch diff show rename binary added modified deleted lines author message",
  },
  {
    id: "git-history-ops",
    section: "Git",
    title: "Undoing and moving commits",
    body: "Right-click a commit in History: copy its id, revert it (a new commit undoes it - the safe choice for work already pushed), cherry-pick it onto the current branch, or move the branch back to it. Reset comes in three flavours the dialog spells out: keep the changes staged, keep them in your files unstaged, or discard them entirely - the last one says plainly that it cannot be undone.",
    keywords: "revert undo cherry-pick reset soft mixed hard rollback commit id sha",
  },
  {
    id: "git-conflicts",
    section: "Git",
    title: "Resolve merge conflicts",
    body: "Conflicted files appear in a red Merge conflicts section - click Resolve to open the conflict view. Each conflict shows Ours and Theirs side by side (plus the common ancestor when available): keep either side, keep both, edit the result by hand, or let AI merge one block - or every block - for you. Save & mark resolved rewrites the file and stages it. Unfinished blocks are never lost: the save button stays locked until every conflict has a decision.",
    keywords: "merge conflict resolve ours theirs ai rebase pull markers",
  },
  {
    id: "git-blame-branch",
    section: "Git",
    title: "Blame & branches",
    body: "The line under your cursor shows who last changed it and when (inline, dimmed, GitLens style). Click the branch name in the Git panel to switch branches or create a new one.",
    keywords: "blame author annotate branch checkout switch create",
  },
  {
    id: "git-stash-amend",
    section: "Git",
    title: "Stash & amend",
    body: "The archive button in the Changes header stashes everything (untracked included, optional message). Stashes list below with Pop (apply & remove), Apply (keep), and Delete, in a section of their own: drag its divider to give the shelf as much room as you want, or fold the heading and it costs a single line. The Amend checkbox next to Commit rewrites the last commit - leave the message empty to keep the old one. A conflicted file open in the editor shows a red banner that jumps straight to the resolver.",
    keywords: "stash save shelve amend rewrite last commit wip",
  },
  {
    id: "git-multiselect",
    section: "Git",
    title: "Select multiple changes",
    body: "In the Staged/Changes lists: click selects a file (and opens its diff), Ctrl+click adds or removes files from the selection, Shift+click selects a range. The branch bar and the commit box stay in place while the file lists scroll under them, and dragging the divider above History gives either side more room - where you leave it is remembered. Right-click for a menu that stages, unstages, or discards everything selected at once. The same menu adds untracked files to .gitignore - one line per file, anchored to that exact path, appended to the project's own .gitignore without touching what is already in it. It is offered for untracked files only, because ignoring a file git already tracks does nothing.\n\nThe file tree's own right-click menu does the same, and it is where a whole folder can be ignored - something the Changes list, which only ever lists files, cannot offer. A folder gets git's trailing slash, so a file that later carries the same name stays tracked. The entry appears on the same terms: untracked things only (a folder counts when it holds one), and never on something already ignored, which is grey on screen and needs no second pattern. A file git already tracks gets a different entry instead - Stop tracking and ignore - because a pattern alone would be read and skipped. It asks first, and says what it costs: Aime takes the path out of git and writes the pattern, the file stays exactly where it is on your disk, and your next commit is what removes it for everyone else.",
    keywords:
      "multi select ctrl shift right click context menu bulk gitignore folder tree untrack stop tracking rm cached",
  },
  {
    id: "terminal",
    section: "Terminal",
    title: "Integrated terminal",
    body: "Ctrl+` (or the status bar button) toggles a PowerShell terminal in the project root. + opens more tabs; every tab is its own shell and hiding the panel never kills it. Ctrl+V and Ctrl+Shift+V paste; the right mouse button copies the selection when there is one and pastes when there is not, as in Windows Terminal - which is how a verification code gets into a sign-in running there.",
    keywords: "shell powershell tabs console",
  },
  {
    id: "task-run",
    section: "Work items",
    title: "Hand a work item over and walk away",
    body: "The sparkle button on a work item no longer sends a prompt into the chat - it starts a run. A run is six steps - the ones a developer already takes - and each ends at a gate something other than the AI can check, because a change is only trustworthy if the thing approving it is not the thing that wrote it. The six are the ones you would name yourself: understand the task and the code it lands in, plan the approach and what proof looks like, write the code and its tests, test it until the bugs are out, check your own work, hand it over - and every claim gets proved against something that is not the author. Before a single token is spent the run starts a branch of its own (never the one you were on), takes a checkpoint so the whole thing can be undone in one click, and runs *every* test command and every check command your project declares, to record what was passing and what was clean *before* - which is what makes every later claim measurable rather than hopeful. A project that declares no test script at all is not given up on: the AI is asked how this project is really tested - read from its CI configuration, Makefile or docs, never invented - and Aime runs every command it names, keeps only the ones that actually ran, and measures them from then on like any declared suite. Then it reads the item into numbered acceptance criteria; a question the ticket leaves open is answered with the sensible default and written into the report rather than parked for a desk nobody is sitting at. It reads the code in the same breath, because what a change is for cannot be settled without the ground it lands on - the files it will touch, and the conventions those files already follow, each one having to name the file it was seen in - and asks the language server which other files use what they declare, the same question 'Find all references' answers, so the list is the real one rather than the model's recollection of it. A file no server can answer for is reported as unknown, never as safe. It also reads the rules your project wrote down - `AGENTS.md`, `CLAUDE.md`, `ARCHITECTURE.md`, `CONTRIBUTING.md`, a Cursor or Copilot rule file - and carries them into deciding, writing and reviewing. Those rules outrank both the AI's defaults and anything it infers from the code, and they are read by Aime rather than left to whichever files a particular CLI happens to pick up. One page then settles the whole shape of the work: how it will be done and why - ONE way, the best one it can see, because handing you three write-ups to choose between hands back the decision you delegated - plus the part a tester would recognise. The criteria turn into numbered test cases: given, when, then, and how each one gets proved. What the right proof is stays the AI's call rather than Aime's guess from a file path, and a case that says nothing about how it would be checked is sent back - a screen is not proved by a unit test of the function behind it. The cases are written into `.aime/test-cases.md` to read and `.aime/test-cases.json` to edit; edit that file and the run works to your definition, not its own. The same page places every case in a test, and is refused if it drops one. By default the run stops there with the approach, the cases and the plan on screen and nothing written yet, which is the last moment where changing direction is free; a switch in the panel runs it to the end instead, once you trust it on that kind of task. Then the code and its tests are written together, in that order, the way you would write them - following your project's architecture as a rule rather than a suggestion, and looking like the rest of your app wherever it touches a screen: your components, your tokens, your spacing, not a look of its own. Performance and security are treated as part of correctness rather than as a later review note, and the OWASP Top Ten is named in what the AI is told rather than left to taste. A run that exits having changed no file is stopped and told so, and so is one whose planned test files never appeared: the plan promised one test per case, so a missing file is a case nobody wrote anything for, and it is asked for again by name. What that order costs is the one check it can no longer make - a test that asserts nothing passes exactly like a test that works - so catching it is given to the reviewer further down, who reads the diff and is told to treat a test that would still pass with the feature deleted as a finding. After that, one phase measures everything and mends what it finds, because a run that reports the damage and leaves has produced homework rather than a task. The cheap gate goes first - your project's own checks, the linter, the type checker, the formatter, fixed and re-run until they pass and never silenced - then every suite against the baseline: a test that was passing and is failing now goes straight back to the AI with the output that proves it and is measured again, up to three rounds, while one that was already red is reported and not blamed on the change. A repair round re-runs only the suites that were failing, and the whole estate answers once more when they come back green - which is what keeps a two-line fix from paying for your nine-minute browser suite three times over. Only what will not mend stops the run. Then a reviewer with a clean context derives the risks of *this* change rather than working a checklist, and looks at four things in every case: your architecture, the OWASP Top Ten where it can apply, performance, and whether a changed screen looks like it belongs in your app. What the reviewer finds is then fixed in the same step and everything measured again, because a review whose findings nobody acts on is a document rather than a gate. Two kinds of finding can end a run outright when they cannot be fixed: architecture and security. Code sitting in the wrong layer does not belong in your repository however well it works, and a hole an attacker can use is not a matter of taste - before this they went into the report while the run reported success, which quietly turned the rule about following your architecture into a suggestion. Everything else a reviewer thinks is reported for you to judge, because a reviewer can simply be wrong. The same step then finishes the way a team calls finished: it builds what your project declares (a build this change broke goes back to the AI with the output attached, up to three rounds), deploys the result the way your project deploys - its own CI file, Dockerfile or script; locally when it declares nothing, and never with invented credentials - smoke-tests what came up, and then drives the RUNNING software through every agreed case, installing what it needs for that (a headless browser, a driver) and showing progress rather than asking permission. What it leaves behind is one artifact per case in `.aime/evidence/`, named after the case - a screenshot, a captured response, the runner's own output - plus proof under `.aime/evidence/deploy/` that the deployed thing answered at all. Aime believes none of it on the AI's word: each file has to exist, be non-empty and be newer than the run, a case with no artifact is reported unproven, and a run with no proof the software ran is stopped, because nothing was delivered. All of that is skipped for a change the run itself judged needs no deployment to be believed - a renamed constant has no running software to photograph, and demanding an artifact for one would either block honest work or be satisfied by a token file, which is worse. That judgement is the AI's, made on the page you approve, from the cases it just wrote: it reads the project and Aime does not. What you come back to is a branch, a table of every test case against whether it was actually proved - and 'proved' still has to be earned: a test naming that case exists in the tree, every suite that answered is green, and - for a change that had to be deployed to be believed - an artifact from the running software names the case; anything less reads as unproven, with the missing condition written in the cell, and the report says which set of conditions this run actually applied - plus whatever evidence the suites left on disk (screenshots, traces, reports), a full report in `.aime/runs/`, and one button to undo all of it. Every run is kept: 'Task runs' in the command palette lists what this project has handed to the AI, opens any of them again, and offers to carry on the one that was cut off mid-phase - a closed lid costs that phase and nothing before it. Beside that history, a finished run has a cleanup section: one click lists what the run left behind - only files it created, told apart by comparing git's untracked list from before the run with the one taken now, plus the artifact folders - and a second click deletes exactly what you ticked. Evidence starts unticked because it proves the work; the cases, the journal and the report are never offered; nothing tracked and nothing that was already there can appear in the list at all. Aime can read failing test names for cargo, vitest and dotnet test today - the last one covering xunit, MSTest and NUnit alike, since all three print through the same runner; for anything else the gates still work on the exit code, and say so rather than pretending to know more. And runs go side by side: hand a second item over while the first is still working and it simply starts, in a git worktree of its own - two runs must never share one checkout - made runnable with the project's own install command, its change committed onto its own branch at the end (the worktree is a workplace; the branch is the delivery), while the first run keeps your tree exactly as before. The panel grows a tab per live run, each with its own log, gates and report; the cleanup of a worktree run also offers the worktree itself, kept by default because the evidence lives inside it.",
    keywords:
      "task run agent autonomous automate six steps phases gate baseline regression acceptance criteria test case QC given when then proof prove solution approach decision plan implement verify quality lint check review deliver build deploy smoke owasp security architecture rules AGENTS.md CLAUDE.md ui style branch checkpoint undo blast radius evidence screenshot artifact history resume report cleanup trash sweep untracked makefile gradle ci discovered coffee unattended",
  },
  {
    id: "work-items",
    section: "Work items",
    title: "Connect a board (Azure DevOps, Jira, ClickUp, GitHub)",
    body: "The checklist tab in the sidebar (or 'Work items panel' in the command palette) shows the work assigned to you on your board and ties it to this repository. Connecting asks only for what the service needs, and the form is built from that: Azure DevOps wants an organization, a project and a personal access token allowed to read and update work items (Azure DevOps Server, on-premises, also takes its own server URL, and naming a team narrows the list to exactly what that team's board holds - the team itself is what defines a board, so Aime asks it rather than inventing a filter). If you work across several projects of one organization, name them all in that field separated by commas: one token, one connection, one list, with a Project heading you can group by - and each item still opens, changes state and takes comments in the project it actually lives in; Jira comes in two entries, because it is two products: Jira Cloud wants your site, the email of your Atlassian account and an API token, while Jira Server / Data Center wants the company URL and a personal access token and no email at all - both leave the project optional, and without one they show your work across the whole site; ClickUp wants only an API token, and a workspace when your account has more than one; GitHub wants a personal access token, a repository if you want to narrow it to one, and an API URL only if your GitHub is your company's own Enterprise Server. GitHub lists issues (never pull requests) with the two states an issue has, and closing one asks which kind of closing it is - done, or not planned. Nothing is stored until a real query has come back, so 'connected' means it works. The board belongs to the project you have open: connecting binds this folder to it, and opening another project shows that project's board or asks which one it uses - one click, since the token is already there. The token itself belongs to you, not to the repository, and is never written inside it. The list files itself the way your board is organized rather than one fixed way: items spread across several boards or several sprints are filed under those, work that mostly sits under something bigger is filed under what it belongs to (a story, a feature, a test suite), and a flat list of independent items is filed by where it stands - in progress, to do, done. The slider button beside 'Mine / All' changes that cut whenever the guess is wrong, and holds the one other thing you set once: whether finished work counts. It also puts your own board first: the heading this repository is about is marked 'this project' and opened before the others, worked out from the repository itself rather than from anything you configure - a branch here named after one of its items (the branch you have checked out counts most, since that is what you are on right now), or a board whose name is the one your folder or your remote answers to. When several boards are on screen and the rest of them add up to more than a screenful, those start folded, counts and all, one click from being open again; fold anything yourself and the panel stops deciding. Every heading folds, carries its count, and shows its first eight rows with the rest one click away, which is what makes a board of forty readable. Each row keeps the team's own words for its type and state, because state names belong to your process template and not to Aime - the colour behind the state is Aime's, and says only the thing every board agrees on: whether that is work in progress, work waiting, or work over. A row gives its title two lines rather than one, since the title is the only part you cannot guess, and it leaves out the type when every item on the board is the same type (GitHub calls them all 'Issue') because a word repeated on every row is a column of noise. The item whose branch you have checked out is marked in the margin and sits at the top of its heading: of everything assigned to you, that is the one you are working on right now, and it should never be something to scroll for. What you set up - the filter, the grouping, the headings you folded - survives a look at another sidebar tab, and resets when you open another project. Clicking a row opens the item in the middle of the window, where an editor tab would be, because that is where there is room to read it: what it sits under, its text rendered from the Markdown every connector hands over (headings stay headings, steps stay a numbered list, code stays in a block, links open in your browser), the short facts the service keeps (who it is assigned to, its priority, what work is left) and its conversation - every comment, and a box to add one that only clears once the board has accepted it. The state beside the title is a menu, so the item can be moved from the same place you are reading it. Above the list, 'Mine' and 'All' decide whose work the board is asked for - yours by default - and the filter box narrows what already arrived (by title or id, accents optional: typing 'dang nhap' finds 'đăng nhập'), which is why it answers as fast as you type. Finished work is left out until you turn on 'Include finished work' in that same options menu, and every service is walked to the same depth: the 300 items you touched most recently. A board with more than that assigned to one person has a triage problem rather than a display one. A row is named by the id you would say out loud - a ClickUp custom id like DEV-123 rather than the internal one it is addressed by - and that is the id a branch is named after too. Per row: open it in the browser, change its state (the list is read from the service, never guessed - on Jira that means the transitions this issue can actually take from where it is, on ClickUp the statuses its list defines), start a branch whose name is suggested from the item and editable before it is created, or hand the item to the AI - its description, acceptance criteria and repro steps travel into the prompt as Markdown, which is the form an agent reads best. When a token expires the panel says so in one line, with a Reconnect button that keeps the project you already typed. The '…' button in the header holds what you do to the board rather than to the list: switch this project to another board you have connected, stop this project from using a board without touching the board itself, or forget the board everywhere, token included - that last one asks before it does anything. The token is kept in Aime's config folder on this machine and is only ever sent to your own service. For the AI to query the board itself, add the matching entry in the MCP catalog - Azure DevOps (which arrives with the organization of the board you connected) or Atlassian for Jira.",
    keywords:
      "azure devops jira atlassian board work item issue task backlog sprint milestone phase pat api token branch state transition comment assigned to me filter search group fold parent story test suite team detail this project relevant repository remote",
  },
  {
    id: "tasks",
    section: "Tasks",
    title: "Run, build and test",
    body: "The play button in the status bar (or 'Run task' in the command palette) lists five outcomes - run, build, test, check, publish - rather than only the commands Aime happens to recognize. Aime works out the common stacks itself and instantly: package.json scripts, cargo, go, dotnet, pytest, docker. For everything else - Maven, Gradle, CMake, Composer, a script your team wrote - the outcome is still exactly what you want, so reading the project is a step INSIDE that entry rather than a second button you have to know about. It happens the moment a project is opened, in the background: the AI reads the repository - manifests, CI configuration, Makefiles, the README - and reports the command for each outcome, naming the file it read it from, so a plausible `mvn package` for a repository with no pom.xml cannot get through. Nothing it says is taken on trust: Aime confirms the tool is installed on this machine and the folder exists in this repository, says which command it had to drop and why, and keeps only the rest. What survives is written into .aime/tasks.json, so the reading is paid for once per repository and you can edit it by hand afterwards. A root that only says how to PACKAGE what is under it - a Dockerfile beside a src/ folder - has its subfolders detected as well, since a Dockerfile says nothing about where the source is. A .NET solution is offered build, test and publish but no run: `dotnet run` takes a project, not a solution, and which of twenty projects to start is a question for the AI rather than a guess from a file name. Every test script is its own task - a repository that keeps `test` apart from `test:e2e` has two suites, and a Task Run measures both rather than half the project - while a script that never terminates (`test:watch`, `test:ui`, `test:debug`) is offered as something to start and watch instead, since a gate that ran one would spend its whole timeout learning nothing. The checks a project runs on itself are listed too: `check`, `lint`, `typecheck`, `format:check`, and `cargo clippy` and `cargo fmt --check` for a Rust crate - only the read-only ones, because `format` and anything `:fix` rewrite the code they are judging. Scripts are run by the manager the project itself declares - npm, pnpm, yarn or bun, taken from its packageManager field first and its lockfile second - so a pnpm project is never handed an npm command. A workspace whose root declares no build or test script of its own is offered the command that runs every member at once. And a repository that builds nothing at the top - a frontend beside an api - has each folder detected separately, listed as 'frontend · npm test' and run in that folder. A task runs in its own terminal tab named after it, so you see the real output and can interact with it. Add or override tasks in .aime/tasks.json (id, label, kind, command, and cwd for a folder below the root) - an entry reusing a detected id replaces it.",
    keywords:
      "task run build test check lint typecheck clippy publish npm pnpm yarn bun workspace monorepo cargo dotnet go pytest docker tasks.json e2e watch",
  },
  {
    id: "tasks-fix",
    section: "Tasks",
    title: "Fix a failure with AI",
    body: "When a task exits with a non-zero code, a red bar appears above the terminal. Fix with AI sends the command, the exit code and the captured output to the AI panel and asks it to find the cause, fix it, and run the task again. Without AI the bar is simply a failure notice you can dismiss.",
    keywords: "fix error failure build failed exit code ai repair",
  },
  {
    id: "ai-setup",
    section: "Editor",
    title: "Open a language Aime does not support yet",
    body: "A bar appears at the top of the editor when the open file's language is missing something an install on this machine would actually fix: a language server (no completions, no types), or the debug adapter Aime launches for that language - debugpy for Python, delve for Go. It also appears when a language server is installed but failed to start - the same silence in the editor, but a different fix: the bar hands the AI the server's own error so it can repair the machine around it (a missing runtime, a broken dependency) instead of reinstalling a package that is already there. It stays quiet everywhere else on purpose. A Markdown, JSON or CSS file has no program to step through, so nothing about debugging is offered there; an adapter Aime downloads itself is one click in Run and Debug instead; and a language Aime drives no adapter for at all needs a new Aime release, not a package, so it would be a promise the editor could not keep. Let AI set it up hands the gap to your AI CLI with the exact brief - which command Aime probes for, which adapter it launches, and the file itself - and the agent reads the project, works out what this machine's toolchain actually needs, installs it and shows you each tool's version to prove it. It runs in its own progress window - not in the chat panel, so it never spends the conversation you are having - under whichever permission level you have set, and it stops to ask rather than push through a licence, a multi-gigabyte download or an administrator prompt. Setting a machine up took over ten minutes in one measured case, so that window has a Cancel button and closing it leaves the run going in the background. When the run succeeds Aime looks again for what it was missing, so the status chip turns green without restarting anything. Install it stays next to it for the case where Aime already knows the single command - that is faster and costs nothing.",
    keywords: "unsupported language install setup ai automatic toolchain missing server debugger",
  },
  {
    id: "debug",
    section: "Debug",
    title: "Run a program with breakpoints",
    body: "Open the file you want to run and press F5, or use Run and Debug (the bug tab in the sidebar). Click to the left of a line number - or press F9 - to set a breakpoint; F5 starts the program and it stops there. The bar in the editor header steps: F10 over a line, F11 into a call, Shift+F11 back out, F5 continues, Shift+F5 stops. While the program is paused the sidebar shows the call stack and the variables of the selected frame, and clicking a frame opens the file it is in. Breakpoints only go into files Aime can actually run - a language it drives an adapter for - so a README or a package.json takes none, and F5 on a file it cannot debug opens Run and Debug with the reason instead of doing nothing.\n\nF5 runs the project's own program, not whichever file is focused. Aime reads the project's manifests for it - a Go package, package.json main or bin, manage.py or main.py, a .NET project that builds an executable - and the button names exactly what it will run. When a repository holds more than one program (a Go service next to a Node front end), the file you are editing picks for you: open something under services/api and that becomes the target, with its own arguments, and moving to services/worker switches both - no trip to the picker. Target still lets you choose by hand, and that choice is remembered for the project; only a file that lives inside another program overrules it, so a shared module or a README leaves it alone. \"assumed\" marks the case where Aime had to guess because the open file belongs to none of them. A project with no entry point Aime can read falls back to the open file, which is also how you debug a one-off script - pick it under Target and that sticks too.\n\nA breakpoint does not have to fire every time. Shift+F9 - or the pencil next to it in the Breakpoints list - opens three fields: stop only when an expression is true, stop after a number of hits (the adapter's own notation: 5, > 5, % 10), or log a message and keep running, which is a logpoint. The debugger evaluates all three, so a condition that is false costs nothing at all. A breakpoint with a rule is drawn hollow, and hovering it says what the rule is.\n\nStop on exceptions lists what the debugger itself offers - js-debug has caught and uncaught, debugpy adds raised and user-uncaught - with each one's own default applied the first time. Aime remembers the list, so the checkboxes are there between runs, and ticking one reaches a program that is already running.\n\nWatch keeps expressions on screen: they are evaluated in the frame you have selected, so picking another frame re-answers them, and a failed one keeps the debugger's own words instead of vanishing. Arguments opens what this program is given - a command line, quotes honoured, and one KEY=VALUE per line for the environment - stored per program in .aime/launch.json so tomorrow's run is the same as today's. Attach to a running program reaches something that is already going: the port `node --inspect` or `python -m debugpy --listen` published. The × beside the port closes that box again, as does Escape.",
    keywords: "debug breakpoint step f5 f9 f10 f11 call stack variables pause continue",
  },
  {
    id: "debug-adapters",
    section: "Debug",
    title: "What Aime can debug",
    body: "JavaScript and TypeScript through js-debug, Python through debugpy, Go through delve, C# and F# through netcoredbg. Nothing is bundled: js-debug and netcoredbg have no package to install, so Aime downloads the pinned release into its own data folder when you press Download it - debugpy comes from your own Python and delve from your own Go toolchain, and the panel shows the exact install command when either is missing. delve is found even when GOPATH/bin is not on PATH, because `go install` is how it is meant to be installed. C# is the one language where starting a run builds first: a CLR debugger attaches to an assembly, not to a .cs file, so Aime builds the project and debugs what came out - the console says Building… while it does. C++ and Java are deliberately absent until their adapters have been driven for real; a language listed here is one that was tested, not one that was read about.\n\nA language that is not on that list can still be debugged, because Aime can be taught. Let AI set it up hands the agent the contract for it: install the adapter this language uses and write `.aime/debug-adapters.json` - how to start it, whether it speaks over pipes or a socket, the fields its launch request needs, any compile step, and a program plus a line to check it against. Aime then checks it itself: it starts the adapter, sets a breakpoint on that line, launches, and only counts the language as debuggable if the program really stops there. Until then Run and Debug says so and offers the check rather than a run - and if the check fails you get the adapter's own words back, with a button that hands them to the AI to fix. Nothing the agent writes marks itself verified; Aime writes that, after it has seen the stop. A project file wins over a machine-wide one, and both lose to a built-in. The offer sits in Run and Debug itself: open a file of a program language Aime has no adapter for - php, ruby, lua - and Let AI set it up is right under the reason, while a document like Markdown, JSON or CSS keeps the plain sentence and no offer at all.",
    keywords:
      "js-debug debugpy delve netcoredbg go python node typescript csharp dotnet adapter download install build",
  },
  {
    id: "debug-console",
    section: "Debug",
    title: "The Debug Console",
    body: "The bug icon on the left of the bottom panel shows what the program printed - stdout in normal text, stderr in red - and the exit code when it ends. Output is joined as it arrives rather than split into rows, because a debug adapter cuts a program's output wherever the pipe happened to flush: one print statement can reach the editor as three pieces. While the program is paused the line at the bottom evaluates an expression in the selected frame - a variable, a call, anything the language allows - and ↑ walks back through the ones already tried.",
    keywords: "console output stdout stderr exit code print evaluate repl expression watch",
  },
  {
    id: "debug-breakpoint-moves",
    section: "Debug",
    title: "Why a breakpoint moved",
    body: "Set one on a blank line or a closing brace and the marker jumps to a nearby line. That is the debug adapter answering: it can only stop on a line that runs, so it reports the nearest one and Aime draws the marker where the program will actually stop rather than where the click was. Not every adapter moves it - delve refuses the line instead and says why, so the dot turns amber and hovering it shows the reason. Breakpoints also follow the text - type above one and it moves with its line - and they are remembered per project between sessions.",
    keywords: "breakpoint moved line verified wrong place persist amber refused",
  },
  {
    id: "error-log",
    section: "UI & shortcuts",
    title: "When something breaks",
    body: "Failures are written to a log file on this machine - the render error that would otherwise leave a blank window, a rejected promise nobody handled - and Settings has a button that opens it. Nothing is sent anywhere. A crash reporter would have been less work, but stack traces carry file paths, project names and sometimes prompts, and an editor that opens private repositories has no business posting those to a third party. If you want to report a problem, the log is there to attach, by your decision rather than by default.",
    keywords: "error log crash report diagnostics privacy blank window",
  },
  {
    id: "updates",
    section: "UI & shortcuts",
    title: "Updates and channels",
    body: "Aime checks once per launch, never again in that session, and downloads nothing until you press Install - an editor that interrupts while you are typing has misjudged its own importance. Settings has two channels: Stable, which is released versions only, and Beta, which also offers pre-releases - newer, and correspondingly less proven. Every build is signed with the project's own key and the signature is checked before anything is installed, whichever channel it came from.",
    keywords: "update upgrade version beta channel stable install signature",
  },
  {
    id: "settings",
    section: "UI & shortcuts",
    title: "Settings",
    body: "Ctrl+, the gear in the status bar, or 'Settings' in the palette opens one page with everything adjustable: theme, language, font size, line wrapping, indent size, which AI CLI and model to use, and what the AI is allowed to do. The same switches also live where they are used - the shield in the AI panel, the theme in the status bar - because the fastest place to change something is where you noticed it. Changes apply immediately.",
    keywords: "settings preferences options font size word wrap theme language ctrl+,",
  },
  {
    id: "ai-composer",
    section: "AI",
    title: "Pointing the AI at the right thing",
    body: "Type @ in the chat box and a file picker opens over it - a few letters of the name are enough, arrows and Enter choose, and the path lands in the message. That is the whole reason it exists: the CLI needs a path, and nobody remembers paths. On an empty conversation the suggestions above the box are picked from what is actually in front of you - the file you have open, the changes you have not committed, tests if this project can run them, a README if it has none - so the first question is one worth asking. Every suggestion is just a prompt: send it, then keep talking normally.",
    keywords: "@ mention file picker path suggestions starter empty chat composer",
  },
  {
    id: "inline-ai",
    section: "AI",
    title: "Suggestions at the cursor",
    body: "Ctrl+Alt+Space asks your AI CLI to finish the line or the block you are on; the answer appears as grey ghost text, Tab accepts it, Esc dismisses it. It reads the code around the cursor, so it suggests something that fits the file rather than something generic. The chip in the status bar shows what it is doing and switches the mode with a click: On ask (the default), Auto - which suggests after a pause in typing - or Off. Auto is not the default on purpose: every suggestion starts your CLI as a process, which measured about seven seconds and roughly a cent per suggestion here, and asking on every pause spends both without being asked. Repeating the shortcut in the same spot is free - the answer is cached. This is separate from the language server's completions on Ctrl+Space, which are instant, local and free; the two answer different questions.",
    keywords: "ghost text inline completion suggestion ctrl+alt+space autocomplete ai copilot",
  },
  {
    id: "ai-actions",
    section: "AI",
    title: "Ask about the code you are looking at",
    body: "Select some code, right-click, and the editor's own menu offers: explain this, clean this up, write tests for it, find and fix bugs, add comments. Aime writes the prompt with the file and the selection in it, so there is nothing to compose and no path to remember. With nothing selected the action takes the whole file. Anything that changes files can be taken back with Undo this turn.",
    keywords: "explain refactor tests fix comments right-click selection context menu ai",
  },
  {
    id: "palette",
    section: "UI & shortcuts",
    title: "Command palette",
    body: "Ctrl+K (or Ctrl+P) opens one box for everything: type to fuzzy-search commands and workspace files together; start with '>' to see commands only. Arrows navigate, Enter runs a command or opens a file, Esc closes.",
    keywords: "quick open search jump command ctrl+k ctrl+p fuzzy",
  },
  {
    id: "panels",
    section: "UI & shortcuts",
    title: "Panels",
    body: "Drag the dividers to resize. Dragging below the minimum collapses a panel into a thin rail with an expand button. Ctrl+B (file tree), Ctrl+L (AI panel), Ctrl+` (terminal) or the status bar buttons toggle them.",
    keywords: "layout sidebar resize collapse rail",
  },
  {
    id: "theme-lang",
    section: "UI & shortcuts",
    title: "Theme & language",
    body: "In the status bar: the sun/moon button switches dark/light mode, EN/VI switches the interface language.",
    keywords: "dark light vietnamese english",
  },
  {
    id: "shortcuts",
    section: "UI & shortcuts",
    title: "All shortcuts",
    body: "Ctrl+K/Ctrl+P command palette · Ctrl+S save · Ctrl+B file tree · Ctrl+L AI panel · Ctrl+` terminal · Ctrl+Shift+N new window · F1 this help.",
    keywords: "keyboard hotkeys keys",
  },
];

const VI: HelpTopic[] = [
  {
    id: "open-project",
    section: "Bắt đầu",
    title: "Mở dự án",
    body: "Ở màn hình chào: Open Folder chọn thư mục bất kỳ, Recent mở lại 8 workspace gần nhất, New Project tạo thư mục mới và mở luôn. Aime đứng ở khoảng trống bên trái danh sách, vẫn lời chào như ở cửa sổ khởi động - cửa sổ hẹp thì không đủ chỗ nên cô ấy lui ra thay vì chen vào các nút.",
    keywords: "workspace recent folder mở thư mục nhân vật",
  },
  {
    id: "updates",
    section: "Bắt đầu",
    title: "Cập nhật",
    body: "Mỗi lần mở app Aime kiểm tra một lần xem có bản mới chưa. Có thì hiện một thanh trên cùng cửa sổ kèm số phiên bản - không tải gì cho tới khi anh bấm Cập nhật ngay, bấm X thì im tới lần mở sau. Bản cập nhật có ký số, nên bản dựng không phải do quy trình phát hành của dự án tạo ra sẽ bị từ chối.",
    keywords: "cập nhật update phiên bản release ký số",
  },
  {
    id: "splash",
    section: "Bắt đầu",
    title: "Cửa sổ khởi động",
    body: "Trong lúc khởi động, Aime chào anh bằng một cửa sổ riêng thay vì bày ra một editor trống rồi mới điền dần. Chính Aime đứng trên tấm thẻ đó: cô ấy ngẩng lên, vẫy tay chào, liếc sang tên mình rồi gật đầu. Lời chào ở lại khoảng bốn giây, và đó không phải thời gian chết - phía sau nó thư mục dự án được mở và toàn bộ bundle của editor được nạp xong, nên thứ thay chỗ lời chào là một editor hoàn chỉnh chứ không phải cái vỏ rồi điền dần. Cô ấy còn chào bằng giọng nói, mỗi lần mở app một lần, theo đúng ngôn ngữ đang đặt cho Aime - đổi ngôn ngữ trong Settings thì từ lần mở sau cô ấy chào bằng ngôn ngữ đó. Giọng đi kèm bản cài nên máy nào cũng giống nhau, không phụ thuộc giọng có sẵn trong Windows. Bấm phím bất kỳ hoặc click là vào thẳng app, giọng nói tắt theo. Nếu khởi động có chuyện, lời chào tự nhường chỗ để editor nói cho anh biết chuyện gì.",
    keywords: "splash khởi động màn hình chào logo bỏ qua đang tải nhân vật vẫy tay hoạt hình",
  },
  {
    id: "cli",
    section: "Bắt đầu",
    title: "Mở từ terminal",
    body: "Gõ `aime .` trong terminal bất kỳ để mở Aime tại thư mục hiện hành, hoặc `aime <đường dẫn>`. App đang chạy sẽ được dùng lại.",
    keywords: "command line launcher dòng lệnh",
  },
  {
    id: "multi-window",
    section: "Bắt đầu",
    title: "Nhiều dự án song song",
    body: "Ctrl+Shift+N, nút cửa sổ trên status bar, hoặc New Window ở màn hình chào - mỗi cửa sổ là một workspace độc lập với AI chat và terminal riêng.",
    keywords: "cửa sổ mới new window",
  },
  {
    id: "switch-close",
    section: "Workspace",
    title: "Đổi hoặc đóng workspace",
    body: "Rê chuột vào tên dự án trên đầu cây file: icon thư mục để mở workspace khác, dấu X để đóng và quay về màn hình chào. Chuột phải vào tên cũng có các lệnh này.",
    keywords: "đóng thư mục quay lại close folder",
  },
  {
    id: "file-tree",
    section: "Workspace",
    title: "Cây file",
    body: "Click mở file; chuột phải có Mở, Mở trong File Explorer, Git Blame (tác giả từng dòng - bấm commit để xem patch), Tạo file, Tạo thư mục, Đổi tên, Xóa. Kéo-thả để di chuyển: thả vào thư mục để đưa vào trong, thả vào file khác để nằm cạnh nó, thả vào tên dự án để đưa ra gốc.",
    keywords: "kéo thả di chuyển tạo xóa đổi tên drag drop reveal explorer blame",
  },
  {
    id: "live-updates",
    section: "Workspace",
    title: "Cập nhật trực tiếp",
    body: "Thay đổi từ bên ngoài editor - kể cả do AI sửa - tự hiện trong cây file và file đang mở, không làm sập trạng thái mở rộng của cây.",
    keywords: "watcher tự động refresh",
  },
  {
    id: "editor",
    section: "Editor",
    title: "Soạn thảo & lưu",
    body: "Ctrl+S để lưu. Chấm tròn cạnh đường dẫn file báo thay đổi chưa lưu; Ctrl+Z về đúng bản đã lưu thì chấm tự tắt. File .csv hay .tsv mở ra dưới dạng BẢNG - header ghim trên đầu, số căn phải, dấu ngăn đọc từ chính file (phẩy, chấm phẩy, tab hay |, vì bảng tính lưu ở locale Việt ghi chấm phẩy mà vẫn gọi là .csv), ô có ngoặc kép chứa phẩy hay xuống dòng đọc đúng, và ô tìm chạm tới mọi dòng dù chỉ vẽ hai nghìn dòng đầu; file .md mở ra thành tài liệu đã vẽ - heading, list, code, bảng, link - và file .json hay .xml (cả .csproj, .plist, .svg và các file XML mang tên khác) thành cây khoá–giá trị gập được, thuộc tính hiện là @tên; file không parse được thì nói thẳng bằng lời của parser. Nút Chữ cạnh tên file cho xem chữ gốc để sửa, và lựa chọn giữ tới khi anh mở file khác. Panel cloud cũng là một tab, mang icon đám mây: mở file lên trên nó thì nó lùi ra sau chứ không đóng, bấm tab của nó là quay lại mà file vẫn nằm sau, đóng file cuối cạnh nó thì nó hiện lại, và chỉ dấu x của chính nó - trên tab hay trên panel - mới đóng nó.\n\nTự động lưu bật sẵn: ngừng gõ một giây là file được ghi xuống đĩa. Một giây đó là cố ý - ghi theo từng phím sẽ kéo theo git status, cây file và khung diff làm mới cho mỗi ký tự. Ở đây nó quan trọng hơn một editor thường, vì agent đọc dự án từ đĩa: chữ mà chỉ cửa sổ này biết là chữ AI không nhìn thấy. Tắt trong Cài đặt thì việc lưu trả về cho Ctrl+S, còn file ghi không được (đang bị khóa, chỉ đọc) vẫn giữ nguyên chấm chưa lưu chứ không âm thầm mất chữ.\n\nCuộn sâu vào trong một hàm thì dòng đầu của nó được ghim lại trên đỉnh editor, để anh luôn thấy mình đang ở trong cái gì. Dòng ghim đó lấy từ outline của file - Monaco tự có cho TypeScript, JavaScript, HTML, CSS, JSON, còn lại do language server trả lời - và file không có outline thì không ghim gì cả, chứ không đoán theo thụt lề (đoán kiểu đó từng ghim ra mấy dòng chỉ có dấu ngoặc).",
    keywords:
      "lưu save dirty undo sticky scroll outline ghim symbol csv tsv bảng table markdown xem trước json xml cây",
  },
  {
    id: "encoding",
    section: "Editor",
    title: "File có dấu BOM ở đầu",
    body: "Visual Studio ghi file C# kèm BOM UTF-8 - 3373 trong 4071 file .cs của solution đo thật đều có - và khi giải mã thì đó là một ký tự thật nằm trước dòng 1. Aime ẩn nó đi, còn khi lưu thì trả lại đúng dấu mà file vốn có, nên mở file ra rồi lưu lại không tự biến thành một thay đổi trong git. Khung diff và patch của commit cũng bỏ nó ra: trong patch, dấu đó nằm sau dấu + của dòng nên trước đây nó bị vẽ thành một ký tự lạ giữa dòng thêm đầu tiên.",
    keywords: "bom dấu thứ tự byte utf-8 encoding visual studio c# diff patch ký tự lạ",
  },
  {
    id: "lsp",
    section: "Editor",
    title: "Code intelligence (gợi ý, kiểu, nhảy tới định nghĩa)",
    body: "Aime nói chuyện LSP với đúng những language server mà VS Code dùng: gõ '.' ra gợi ý thật, rê chuột thấy kiểu và tài liệu, F12 nhảy tới định nghĩa, Shift+F12 liệt kê mọi chỗ dùng, F2 đổi tên biến/hàm trên toàn dự án - file chưa mở được ghi thẳng xuống đĩa, file đang mở nhận sửa đổi bình thường và undo được - gợi ý tham số hiện trong ngoặc hàm, lỗi gạch chân ngay khi gõ. Aime tô màu sẵn ~80 ngôn ngữ - phần đó đóng gói kèm - và có IntelliSense đầy đủ cho TypeScript, JavaScript, HTML, CSS, JSON. Còn lại cần language server, mỗi cái 20-200 MB nên không gói kèm. Thay vào đó lần mở đầu tiên Aime tự lặng lẽ cài nhóm nhỏ chạy trong thư mục người dùng (Python, PHP, SQL, shell, YAML, Dockerfile) mà không hỏi, vì đó là thoả thuận khi dùng Aime; và khi mở file thuộc ngôn ngữ chưa có server thì đề nghị ngay tại chỗ. Nó chạy trình cài thật và hiện từng dòng. Không cài gì thì vẫn có tô màu cú pháp cho mọi ngôn ngữ, và IntelliSense đầy đủ cho TypeScript, JavaScript, HTML, CSS, JSON vì Monaco có sẵn. Chip ngôn ngữ trên status bar cho biết trạng thái: xanh là server đang chạy, vàng là chưa cài và tooltip ghi sẵn lệnh cài. Riêng TypeScript/JavaScript vẫn có IntelliSense sẵn của Monaco kể cả khi chưa cài server.\n\nMười ngôn ngữ phải chạy được ngay sau khi cài Aime: Go, Node, TypeScript, JavaScript, HTML, CSS, Python, Rust, C# và SQL. Bốn trong số đó không cần gì cả, còn server cho Python, SQL, YAML, Dockerfile, PHP và shell thì tự cài ngay lần chạy đầu - chúng là package npm, vào thẳng prefix của anh trong vài giây. Server cho Go và Rust giờ cũng tự cài, nhưng chỉ khi toolchain đã có sẵn trên máy: `go install` mà không có Go là thất bại đội lốt lời mời, nên trường hợp đó không hiện lệnh trần nào, để 'Để AI lo' xử lý.\n\nC# giờ dùng Roslyn language server - đúng cái extension C# của VS Code chạy. Nó không có package để cài nên Aime tự tải (65 MB) khi anh bấm, hiện từng dòng trong lúc tải, rồi nói cho nó biết đang xem project hay solution nào - thiếu bước đó thì nó không phân tích gì cả. Đây là server duy nhất Aime không tải ngầm, vì 'cài ngầm' phải luôn có nghĩa là vài giây. Khi Roslyn xin package NuGet của project, Aime tự chạy dotnet restore ngầm - chip ngôn ngữ quay trong lúc đó, và restore xong thì gợi ý đầy đủ ngay. Lần restore đầu của solution lớn có thể mất vài phút; đã restore rồi thì chỉ vài giây.\n\nJava dùng Eclipse JDT LS, và nó chạy được trên một thư mục .java trần - không cần Maven hay Gradle, vì server tự dựng project riêng cho các file rời. Nó là một bộ jar chứ không phải một chương trình, nên Aime chạy nó bằng JVM có sẵn trên máy: nếu máy có JDK 17 trở lên, mở file .java sẽ thấy lời mời tải (45 MB) và chip xanh sau vài giây - JDT LS phải khởi động JVM rồi lập chỉ mục thư mục trước khi trả lời, nên khoảnh khắc đầu chậm hơn các server khác. Không có JDK thì không mời gì cả, vì 45 MB jar không hoàn thành nổi một biểu thức khi thiếu JVM, và cài JDK chính là việc của 'Để AI lo'. Nếu máy đã được dựng sẵn để debug Java thì không phải tải nữa: debug adapter chạy bên trong JDT LS, nên bản đang có phục vụ cả hai việc.",
    keywords:
      "lsp intellisense gợi ý autocomplete hover định nghĩa f12 lỗi chẩn đoán gopls pyright rust-analyzer",
  },
  {
    id: "plugins",
    section: "Editor",
    title: "Plugin",
    body: "Một plugin là thư mục có plugin.json và một script, nằm trong thư mục dữ liệu của Aime - trong Cài đặt có nút mở thư mục đó. Nó tắt cho tới khi anh bật, và chạy trong worker riêng: không có DOM, không với được vào ruột Aime, và chỉ dùng đúng những capability mà manifest xin (editor, files, ui). Gọi ra ngoài phạm vi đó bị từ chối kèm câu nói rõ thiếu quyền gì. Lệnh của nó xuất hiện trong bảng lệnh như mọi lệnh khác, và thay đổi nó áp vào file đi qua editor nên Ctrl+Z lấy lại được.\n\nSandbox tồn tại vì lỗi vô tình nhiều hơn vì ý xấu: plugin lặp vô tận thì chỉ chiếm một core, cửa sổ vẫn mượt; khởi động quá năm giây thì bị dừng và nói rõ; gọi Aime hơn một trăm lần một giây thì bị dừng vì đó là vòng lặp - con số đó được đếm ngay trong worker của plugin, nên lệnh vượt giới hạn báo lỗi tại chỗ và cơn lũ dừng ngay từ nguồn; output có giới hạn dòng chứ không phình mãi. Plugin nào cũng dừng tay được từ Cài đặt. Và Aime không bao giờ nạp plugin từ dự án, vì mở repo của người khác thì không được phép chạy code của họ.",
    keywords: "plugin tiện ích api sandbox worker quyền lệnh",
  },
  {
    id: "ai-requirements",
    section: "AI",
    title: "AI cần gì (và không có AI vẫn dùng được gì)",
    body: "AI chạy qua một AI CLI cài trên máy bạn: npm install -g @anthropic-ai/claude-code cho Claude Code, npm install -g @openai/codex cho Codex. Aime tự dò CLI đang chọn: chưa cài thì hiện lệnh cài, chưa đăng nhập thì hiện nút đăng nhập một chạm. Không có AI thì editor, cây file, terminal và Git vẫn chạy bình thường.",
    keywords: "cài đặt đăng nhập login yêu cầu offline thủ công không có ai codex claude",
  },
  {
    id: "ai-provider",
    section: "AI",
    title: "Chọn AI CLI & đăng nhập",
    body: "Chip đầu tiên trên header panel AI chuyển giữa Claude Code và Codex. Đổi provider sẽ mở phiên mới vì id resume thuộc về đúng một CLI - các phiên cũ vẫn giữ CLI đã tạo ra chúng. Nút chìa khóa bên cạnh dùng để đăng nhập: nó mở một tab terminal chạy đúng lệnh login của CLI đó (claude auth login / codex login), nên thông tin đăng nhập nằm ở CLI, không đi qua Aime. Đăng nhập rồi thì nút chuyển màu xanh, bấm vào chỉ báo là đã đăng nhập; sau khi login xong Aime tự nhận, không cần bấm gì thêm.\n\nVới provider hỗ trợ, có thể dùng API key thay cho đăng nhập. Dán key vào Cài đặt > AI > API key (thông báo yêu cầu đăng nhập cũng có nút dẫn tới đó): key chỉ lưu trên máy này, được truyền vào các lần Aime tự chạy CLI qua biến môi trường (ANTHROPIC_API_KEY cho Claude Code), và không bao giờ hiện lại sau khi lưu - muốn đổi thì xoá rồi dán key mới. Khi có key, mọi lượt Aime chạy sẽ ưu tiên key hơn login của CLI, nhưng terminal của bạn thì không bị ảnh hưởng. Codex cũng nhận key ở đúng ô đó nhưng đi đường khác, và phần mô tả nói rõ: CLI của nó tự giữ key, nên Aime đưa key cho codex login --with-api-key rồi không giữ bản sao - việc này thay luôn tài khoản Codex đang đăng nhập, nên Aime hỏi xác nhận trước, và chỉ codex login mới đưa lại được như cũ. Nhận key xong, Aime hỏi lại chính CLI xem nó đang dùng gì rồi báo đúng câu trả lời: đã xác nhận, hoặc không dùng key nào cả (đúng dấu hiệu gõ sai tên biến trong providers.json), hoặc không biết - có CLI không nói gì về key, và Aime nói thẳng là không xác nhận được chứ không vờ như đã xong. Không CLI nào kiểm tra key đúng hay sai ngay lúc dán, nên key sai vẫn chỉ lộ ra ở lượt trả lời đầu tiên.",
    keywords: "provider đổi codex claude đăng nhập login chìa khóa tài khoản api key anthropic token",
  },
  {
    id: "ai-add-provider",
    section: "AI",
    title: "Dùng AI CLI khác",
    body: "Claude Code và Codex có sẵn, nhưng anh thêm được bất kỳ AI chạy dòng lệnh nào mà không cần chờ bản Aime mới: lệnh 'Thêm AI CLI' trong command palette tạo sẵn providers.json có mẫu chạy được và mở lên. Anh khai lệnh chạy, chỗ đặt prompt ({prompt}), cách nối lại hội thoại ({sessionId}), và nó in text thường hay JSON mỗi dòng. Xong là CLI đó hiện trong picker như hai cái có sẵn. Aime không thể biết model hay cờ phân quyền của nó, nên gửi thứ mọi CLI đều hiểu: chỉ dẫn nằm ngay trong prompt - kèm cả AGENTS.md của anh nếu CLI đó không có file memory riêng. Nếu CLI của anh đọc được prompt từ standard input, hãy đặt promptStdin là true và bỏ {prompt} khỏi args. Trên Windows đây không phải chuyện sở thích: CLI do npm cài là một shim .cmd, chỉ cmd.exe chạy được nó, mà cmd.exe cắt dòng lệnh ngay ký tự xuống dòng đầu tiên - đo thật thì shim nhận được danh sách tham số RỖNG và vẫn exit 0, tức CLI trả lời một prompt nó chưa từng nhận và lượt chạy trông như thành công. Aime chặn thẳng tổ hợp đó và nói rõ phải sửa cấu hình nào, thay vì để model trả lời từ hư không. CLI là executable thật thì được spawn trực tiếp, tham số nguyên vẹn, nên không vướng giới hạn này. Hai provider có sẵn thì dùng stdin bất kể thế nào. Mà anh cũng không cần tự viết: Cài đặt > AI > Thêm AI CLI chỉ cần tên CLI rồi giao việc cho agent - nó đọc help của chính CLI đó, chạy thử một lần xem nó in ra gì, rồi ghi cấu hình hộ anh, chạy trong cửa sổ tiến trình riêng chứ không chiếm hội thoại của anh. Kiểu nào thì Aime cũng theo dõi providers.json và đọc lại ngay khi file đổi, nên CLI mới hiện trong picker mà không cần khởi động lại; lỡ file hỏng JSON thì Aime giữ nguyên hai provider có sẵn, chỉ bỏ qua phần cấu hình.",
    keywords: "providers.json generic adapter gemini cli tùy chỉnh thêm provider nạp lại khởi động",
  },
  {
    id: "ai-chat",
    section: "AI",
    title: "Chat với AI",
    body: "Mở thư mục xong, gõ yêu cầu và Enter. AI đọc/sửa dự án và chạy lệnh; tool hiện thành chip, chi phí và thời gian hiện sau mỗi lượt. Mỗi lượt còn mang theo cái anh đang nhìn, nên hỏi 'cái này làm gì?' hay 'lấy thông tin kết nối của cái này' không cần chỉ tay: file đang mở phía trước (dạng @ để CLI đọc), dòng con trỏ và đoạn đang bôi chọn, các tab khác đang mở, và view đang chiếm vùng editor - diff, commit, work item, task run, hay panel cloud tới tận cloud, tài khoản, resource đang mở và cấu hình đã đọc của nó. Lệnh đọc có thể mang credential không bao giờ được đưa vào, dù anh đã bấm Hiện hay chưa; và khối này đi kèm nội dung gửi đi, không hiện thành lời của anh trong khung chat. Câu trả lời được vẽ đúng theo Markdown mà AI viết - heading, list, khối code, quote, link, đậm nghiêng, và bảng kẻ ống kèm căn cột - còn thứ anh gõ thì giữ nguyên như gõ; không gì trong câu trả lời biến được thành markup thật, một thẻ script chỉ là mấy chữ script. Câu trả lời được gõ ra màn hình theo đúng nhịp nó tới: đo với CLI thật, chữ về Aime theo cụm vài dòng mỗi ba phần tư giây, nên mỗi cụm được trải ra trong khoảng chờ cụm kế tiếp thay vì nhảy một cục - và thứ gì không phải chữ, một chip tool hay dấu kết lượt, sẽ hiện hết phần chữ còn chờ trước, nên không bao giờ sai thứ tự. Trong lúc lượt đang chạy, khung chat nói rõ đang chờ kiểu gì: ba dấu chấm kèm đồng hồ đếm giây khi chưa có chữ nào, con trỏ nhấp nháy sau phần chữ đang chảy tới, và spinner trên chip tool khi lệnh của nó đang chạy - lượt xong thì cả ba biến mất. Nút Stop hủy lượt đang chạy. Shift+Enter để xuống dòng, và thanh chia phía trên ô nhập kéo được để ô cao lên khi yêu cầu dài cả đoạn - kéo tới đâu Aime nhớ tới đó.",
    keywords: "prompt gửi claude agent kéo chiều cao ô nhập chờ hiệu ứng dấu chấm con trỏ spinner đang chạy",
  },
  {
    id: "ai-model",
    section: "AI",
    title: "Model & effort",
    body: "Hai chip trên khung chat chọn model và mức suy luận của CLI đang dùng. Với Claude, alias như Opus (latest) luôn theo bản mới nhất, bản pin (vd Opus 4.8) đứng yên; với Codex là danh mục model của nó (GPT-5.6 Sol, Terra, Luna…). Danh sách effort bám theo model đã chọn - chỉ hiện mức model đó chấp nhận. Auto = để cấu hình của chính CLI quyết định.",
    keywords: "opus sonnet haiku fable gpt codex phiên bản effort ultra",
  },
  {
    id: "ai-approve",
    section: "AI",
    title: "AI được làm tới đâu (quyền)",
    body: "Chip khiên xoay vòng 3 mức: TOÀN QUYỀN - mọi tool chạy không hỏi; SỬA FILE - sửa file thoải mái còn lệnh chạy trong sandbox giới hạn ở dự án này; CHỈ ĐỌC - AI đọc và giải thích nhưng không thay đổi gì, hợp khi mới vào một codebase lạ. Lựa chọn được ghi nhớ và áp dụng cho cả hai CLI, ánh xạ sang đúng cờ của từng CLI. Aime cố ý không có popup Allow/Deny từng lệnh: nó cần đóng gói thêm một sidecar Node mà chỉ một CLI hỗ trợ, đánh đổi bằng dung lượng và tốc độ của app.",
    keywords: "permission quyền sandbox chỉ đọc an toàn khiên",
  },
  {
    id: "ai-usage",
    section: "AI",
    title: "Mức dùng & chi phí",
    body: "Nút activity trên khung chat hiển thị tổng của phiên: token vào/ra, cache đọc/ghi, kèm chi phí với CLI nào có báo giá. Claude Code báo chi phí từng lượt; Codex tính theo gói thuê bao và không báo giá, nên Aime chỉ hiện token thay vì bịa ra con số.",
    keywords: "token chi phí tiền usage thuê bao",
  },
  {
    id: "ai-undo",
    section: "AI",
    title: "Lấy lại một lượt AI",
    body: "Để AI sửa dự án chỉ thoải mái khi việc sửa ngược lại chỉ tốn một cú bấm. Dưới mỗi câu trả lời có đụng vào file, Aime nói rõ nó sửa bao nhiêu file và cho nút 'Hoàn tác lượt này' - đưa từng file về đúng như trước lúc anh gửi tin nhắn, kể cả file do AI tạo ra cũng bị xoá đi. Phần anh đã stage vẫn nguyên, và những sửa đổi của chính anh trước lượt đó vẫn được giữ. Việc này cần dự án là repo git; không có git thì Aime không hiện nút hoàn tác, thay vì hứa một điều không giữ được.",
    keywords: "hoàn tác undo rollback checkpoint sai an toàn ai sửa file",
  },
  {
    id: "ai-sessions",
    section: "AI",
    title: "Phiên & bộ nhớ",
    body: "Hội thoại được lưu theo từng dự án và khôi phục khi mở lại - AI tiếp tục với đầy đủ ngữ cảnh, kể cả sau khi tắt app. CLI anh chọn cũng nằm trong đó: mở lại dự án là tiếp tục hội thoại mới nhất của đúng CLI đó, không có thì mở phiên mới chứ không tự đổi CLI sau lưng anh - một hội thoại không thể đổi CLI giữa chừng vì mã resume thuộc về CLI đã tạo ra nó. Nút lịch sử chuyển giữa 20 phiên gần nhất, mở lại phiên cũ thì CLI của phiên đó quay lại theo; dấu + tạo phiên mới. AI còn tự ghi nhật ký tiến độ .aime/PROGRESS.md nên task dài không bao giờ mất dấu. Thư mục đó là ghi chép của Aime về dự án chứ không thuộc dự án, nên nó tự loại mình ra: lần đầu xuất hiện, Aime ghi .aime/.gitignore và git thôi nhìn thấy thư mục này, không cần đụng vào .gitignore của anh. Anh sửa file đó thì nó thuộc về anh - Aime không bao giờ ghi đè.",
    keywords: "resume lịch sử khôi phục ngữ cảnh nhớ tiến độ gitignore commit",
  },
  {
    id: "ai-memory-editor",
    section: "AI",
    title: "Sửa bộ nhớ của AI",
    body: "Nút hình não trên khung chat (hoặc 'Sửa bộ nhớ AI' trong command palette) mở trình sửa memory. Tab Dự án sửa một file AGENTS.md duy nhất trong repo (quy ước, kiến trúc - chia sẻ qua git): Codex đọc thẳng, còn Aime giữ dòng import @AGENTS.md trong CLAUDE.md để Claude đọc đúng file đó - không nhân bản nên không bao giờ lệch nhau. Tab Toàn cục sửa file người dùng của chính CLI đang chọn (~/.claude/CLAUDE.md hoặc ~/.codex/AGENTS.md). AI đọc lại mỗi lượt nên lưu xong là có hiệu lực ngay.",
    keywords: "memory agents.md claude.md toàn cục dự án kiến thức ghi chú não",
  },
  {
    id: "cloud",
    section: "AI",
    title: "Kết nối cloud, và cho AI biết ở đó đang có gì",
    body: "Nút cloud trên thanh trạng thái mở ra một console riêng, bố cục đúng như mọi console cloud: các cloud là tab phía trên - Azure, AWS, Google Cloud, Supabase - cột TÀI KHOẢN mà CLI của cloud đó đang giữ nằm bên trái, và tài khoản đang chọn chiếm phần còn lại. Lần đầu mở, panel hỏi anh thật sự dùng những cloud nào trong bốn cloud đó, vì một tab của cloud người khác là một tab chắn đường: tick cloud anh cần, câu trả lời được nhớ cho máy này. Mỗi tab có dấu x riêng - bấm chuột giữa cũng được - và dấu cộng ở cuối dải tab đưa cloud nào cũng quay lại; đóng tab chỉ là đóng tab, nên cloud trở lại vẫn đang đăng nhập và vẫn còn nguyên những gì đã đọc được, còn đóng tab cuối cùng thì panel hỏi lại câu đó. Liệt kê MỌI tài khoản chứ không phải một: đo trên máy làm ra tính năng này, `az` giữ bốn subscription của hai user và hai tenant, `aws` giữ 28 profile, nên cột tài khoản là một mặt điều hướng thật, có ô lọc riêng. Danh sách tài khoản của Azure và AWS đọc từ phần CLI lưu trên đĩa, nên mở hai tab đó không gọi cloud lần nào; với Google Cloud, tài khoản là PROJECT, xếp dưới tài khoản Google nhìn thấy nó, và vì `gcloud` không lưu danh sách project trên đĩa nên tab đó gọi một lần `gcloud projects list` cho mỗi tài khoản đã đăng nhập khi mở, rồi giữ kết quả. Chọn một tài khoản là lần gọi MẠNG DUY NHẤT - mỗi cloud một lệnh generic, `az resource list`, `aws resourcegroupstaggingapi get-resources` hoặc `gcloud asset search-all-resources` (Supabase là ba lệnh liệt kê của nó), hiện ngay trên màn hình trong lúc chạy - và kết quả được giữ lại, quay về tài khoản đã xem không tốn gì. Nó mang về tới mười nghìn resource kèm tag; tài khoản thật đầu tiên đo được có 6.252. Tài khoản mở ra dưới dạng RESOURCE - bảng gom nhóm những gì đang có trong đó - vì đó là thứ người ta mở console lên để xem; chế độ Ứng dụng nằm ngay cạnh, trả lời câu còn lại 'trong mười app này cái nào là cái nào', cách một cú bấm. Trong chế độ đó panel tự chọn, từ chính dữ liệu, tag key chia tài khoản thành ứng dụng tốt nhất - không phải một danh sách key ưa thích cố định, chính vì thế mà tag `Area` của team từng bị bỏ qua - và nói rõ nó dùng key nào trong một dropdown để anh chọn key khác, stack CloudFormation mà cloud ghi trên từng resource, hay resource group. Các ứng dụng nằm thành một cột bên trái, lớn nhất trước, mỗi cái có một thanh nói nó gồm những gì; bên cạnh, phần tài khoản trả lời đúng cái cột đó không nói được - bao nhiêu resource không có tag nào nói thuộc app nào (một cú bấm mở đúng nhóm đó ra), account này thật sự chạy trên những service nào, mỗi loại một thanh, và ở những vùng nào. Mở một ứng dụng ra thì nó được vẽ thành sơ đồ: ba làn từ trên xuống - nơi nhận request, nơi chạy code, nơi lưu dữ liệu - phần xung quanh (log, quyền, registry) nằm dưới. Ba làn LUÔN được vẽ, nên một làn trống cũng là một thông tin: làn ghi 'chưa có gì ở đây' là cách một back-end chạy theo trigger, không có endpoint và không có database riêng, tự nói ra trong một cái nhìn. Mỗi LOẠI resource là một ô mang số lượng, instance nằm bên trong, và mũi tên là luồng theo tầng đó chứ không phải phụ thuộc đo được - phụ thuộc thật nằm trong cấu hình của từng service, mở detail của resource là đọc được. Chế độ Resource là cùng tài khoản đó dưới dạng bảng gom nhóm - theo loại, nhóm hay vùng, nhóm gập sẵn nên sáu nghìn dòng mở ra thành bốn mươi tiêu đề kèm số, dải chip để thu về một loại, gập hết và mở hết - và ô tìm tìm trên mọi thứ: tên, loại, nhóm, vùng, giá trị tag. Bấm một resource là nó mở phủ lên: Tổng quan, Kết nối, Triển khai cùng, JSON gốc. Cấu hình trong đó đến từ chính CLI, qua các lệnh đọc mà AI LẬP KẾ HOẠCH MỘT LẦN CHO MỖI LOẠI resource - Aime hỏi cách đọc một Lambda function, một App Service hay một Cloud Run service, đưa cho AI hình dạng định danh đã che account và tên, và với AWS là chính danh mục lệnh của service đó đọc từ model nằm cạnh binary của CLI - rồi Aime KIỂM từng lệnh trước khi giữ: với AWS, operation và mọi cờ phải có trong service model của chính CLI; với Azure và Google Cloud, `--help` của chính CLI phải nhận lệnh đó (đo được: exit 0 với lệnh thật, 2 với lệnh gõ sai); phải là lệnh đọc (describe, get, list, show), phải mang đủ mọi cờ mà chính synopsis của CLI ghi là bắt buộc - lỗi báo ngày 2026-09-10: `gcloud logging buckets describe` được giữ mà thiếu `--location`, nên mọi bucket loại đó đều trả *argument --location: Must be specified*, vì lệnh có tồn tại chỉ là thứ duy nhất mã thoát chứng minh được - không được mang các cờ Aime tự thêm, và không giá trị nào được chứa thứ shell có thể hiểu sai. Rồi Aime CHẠY thật lệnh đó một lần trên chính resource anh vừa mở - vì lệnh có tồn tại không có nghĩa là lệnh chạy được, mà kế hoạch lưu theo LOẠI, nên một lệnh không gọi đúng được resource sẽ là câu trả lời của cả loại đó trên máy này mãi mãi: đo trên tài khoản Google thật ngày 2026-09-11, `gcloud iam service-accounts describe` được đưa đường dẫn tương đối trong khi nó nhận email, và trả về trang 404 cho cả 15 service account; `gcloud logging buckets describe` cũng vậy với cả 26 log bucket. Lệnh đọc nào hỏng thì được đưa lại cho AI kèm nguyên lời CLI - phần thuộc về resource được thay lại bằng placeholder trước, nên AI vẫn không hề thấy resource nào của anh - và hỏi lại, nhiều nhất hai lượt; thứ vẫn không chạy thì bị bỏ chứ không lưu, kèm câu nói của CLI bên cạnh. Lỗi không phải tại câu lệnh thì được tách riêng: project tắt API, chưa gắn billing hay đăng nhập hết hạn đều từ chối lệnh ĐÚNG y hệt như vậy, nên lệnh đó được giữ, nguyên lời CLI hiện ở chỗ đáng ra là kết quả - kèm nút bật API, đúng cái nút mà màn liệt kê đưa ra - và kế hoạch được ghi là chưa chứng minh để lần mở sau thử lại. Lệnh đọc có thể mang credential thì không bao giờ bị chạy để chứng minh: nó vẫn chờ cú bấm của anh như từ trước tới nay. Cái gì qua được thì ghi xuống đĩa, nên resource đầu tiên của một loại chờ vài giây cho kế hoạch, các resource sau không chờ, và kế hoạch tính theo máy chứ không theo dự án. Tên đưa vào câu lệnh luôn là tên riêng của resource, không phải nhãn hiện trên màn hình - đo được: một phần tư số resource của tài khoản đó trả về nhãn kiểu `Default compute service account` trong khi lệnh cần email hoặc id. Azure luôn bắt đầu từ `az resource show`, lệnh trả lời cho mọi loại. Google Cloud là HAI CLI chứ không phải một: `gcloud` không có lệnh `bigquery` nào cả, nên dataset BigQuery được đọc bằng `bq` - CLI riêng của BigQuery, nằm cùng thư mục với `gcloud` trong Cloud SDK nên không phải cài thêm gì - và AI ghi rõ tên nó trên lệnh đọc. `bq` chỉ được dùng cho BigQuery, và không một CLI thứ ba nào được dùng. Có những loại không CLI nào đọc được: đo trên một tài khoản Google thật, bảy trong hai mươi mốt loại không có lệnh `gcloud` nào - app và project Firebase, ruleset và release của security rules, nhà cung cấp danh tính, khoá service account - và ba trong số đó thuộc dịch vụ mà `gcloud` không có cả nhóm lệnh. Aime tự đọc chúng thay vì bắt bạn cài thêm: Cloud Asset Inventory, đúng API đã liệt kê tài khoản, trả về nguyên thân resource khi được hỏi, nên những loại đó giờ mở ra là thấy cấu hình thật - package name và app id của một app Firebase, nguyên source của một ruleset, thuật toán và hạn dùng của một khoá. Nó chỉ được thêm vào chỗ AI không đưa ra được lệnh đọc, vẫn được chạy thử thật như mọi lệnh khác, và phần tìm kiếm bọc ngoài được bóc đi để thứ bạn thấy là resource chứ không phải kết quả tìm kiếm. Kế hoạch đọc ghi từ trước khi có phần thông tin kết nối được coi như loại chưa ai hỏi, và được lập lại ngay lần mở resource đầu tiên: trên máy làm ra bản này, tám trong mười sáu kế hoạch nằm ở dạng đó - có câu lệnh mà không có thông tin kết nối nào - và một trong tám cái là rỗng, tức không hiện được gì mà cũng không bao giờ được hỏi lại. Mỗi lệnh đọc hiện đúng câu lệnh mà developer copy về tự chạy được, kết quả là bảng thuộc tính chứ không phải một khối chữ, phần lồng nhau gập lại, mỗi giá trị một cú bấm là copy; tab Kết nối rút ra mọi thứ payload gọi là endpoint, host, port hay URL và đặt lên đầu. Phía trên đó là thứ không cần gọi gì hết: đo trên một tài khoản Google thật, với phần lớn những gì người ta có - topic Pub/Sub, bucket, dataset BigQuery, app App Engine - thì 'thông tin kết nối' CHÍNH LÀ định danh của resource viết theo cách SDK nhận, mà Aime đã có đủ mọi mảnh từ lúc liệt kê. Nên AI được hỏi luôn phần đó, một lần cho mỗi loại, dưới dạng khuôn như `projects/<project>/topics/<name>`; Aime điền vào theo từng resource rồi đặt dưới mục 'Dán mấy cái này vào app', mỗi dòng một cú bấm là copy, không gọi mạng và không phải chờ. Khuôn nào không có chỗ trống thì bị từ chối, vì nó sẽ nói y hệt nhau cho mọi resource cùng loại; và credential thì không bao giờ nằm ở đây - key không thể suy ra từ cái tên, nên nó vẫn là lệnh đọc nằm sau nút Hiện. Thứ gì có thể mang credential - key, connection string, publishing credential - Aime tự đánh dấu bất kể AI gọi nó là gì, không bao giờ chạy cho tới khi anh bấm Hiện đúng lệnh đó, và kết quả chỉ nằm trên màn hình: không ghi vào file, không đưa cho AI, không đưa vào prompt. Đăng nhập: tài khoản Azure hết hạn token hiện nguyên lời CLI - đo được, `az account list` trả từ cache còn `az resource list` lỗi `Status_InteractionRequired` - và nút Đăng nhập chạy `az login --use-device-code` cho đúng tenant của tài khoản đó. Luồng device-code là thứ giữ trình duyệt nằm TRÊN cửa sổ editor: trước đây CLI tự mở trình duyệt từ một tiến trình cách Aime vài tầng nên trang đăng nhập chìm xuống dưới; giờ CLI in ra trang và mã, Aime tự mở trang từ tiến trình của chính nó, hiện mã to và chép sẵn vào clipboard, rồi tải lại tài khoản khi CLI báo xong. Aime không thấy credential nào - chỉ cái mã công khai dùng một lần đó. Google Cloud làm cùng ý đó nhưng theo chiều ngược: `gcloud auth login --no-launch-browser` in ra trang đồng ý thay vì tự mở, Aime mở trang lên trên, và khi trang hiện mã xác minh thì anh dán mã vào ô trong panel - mã đi vào stdin của CLI và không đi đâu khác; vì mã do trang hiện ra chứ không trả về máy này, nên khi trình duyệt máy này bị chặn đăng nhập Google thì sao chép link mở trên điện thoại hay máy khác vẫn được, rồi panel tải lại project khi CLI báo xong. Profile AWS đăng nhập theo đúng cách config của nó nói: `aws configure` cho profile thật sự giữ key (profile role chỉ về source profile của nó), `aws sso login` cho profile SSO - và những lệnh đó chạy trong tab terminal, vì key gõ ở prompt phải nằm trong một shell thật mà Aime không đọc. Đăng nhập bằng cách nào cũng được - nút trong panel, tab terminal, hay một shell riêng của anh - panel tự biết: mọi lần đăng nhập đều kết thúc bằng việc CLI ghi lại file của chính nó (`~/.azure/azureProfile.json` và token cache, `~/.aws/credentials` và `config`, token dưới `~/.aws/sso/cache`, `credentials.db` và các configuration của `gcloud` dưới `%APPDATA%\\gcloud` trên Windows và `~/.config/gcloud` ở nơi khác), Aime theo dõi các file đó trên cả Windows, macOS và Linux, và khi một file đổi ở cloud đang hiện trạng thái chưa đăng nhập thì nó probe lại cloud đó, đọc lại tài khoản và chạy lại mọi listing mà CLI đã từ chối - chấm đỏ thành xanh, bảng tự đầy, không cần bấm Thử lại. Tài khoản đã liệt kê xong thì để yên: token âm thầm được làm mới giữa một lần liệt kê cũng ghi lại đúng file đó, và đó không phải lý do để gọi cloud thêm lần nữa. Chuyển tài khoản trong panel không đổi gì ngoài Aime: mọi lệnh đọc mang thẳng subscription, profile, project và region, còn ghi đè default mà terminal của anh dùng là hành động riêng, chủ động - ngôi sao cạnh subscription Azure hay project Google Cloud (`az account set`, `gcloud config set project`; AWS không có default kiểu đó). Cloud chưa có CLI thì xin cài giúp và probe lại ngay khi cài xong (winget trên Windows, Homebrew trên macOS và Linux có brew, trang của hãng nếu không đâu có; Supabase tải thẳng binary release vì Windows không có package), Lệnh liệt kê lỗi thì được ĐỌC chứ không chỉ hiện ra: nguyên lời CLI vẫn ở đó, nhưng hành động đưa ra là hành động đúng với lời đó - nút Đăng nhập chỉ hiện khi CLI thật sự nói về đăng nhập, còn khi Google Cloud từ chối vì một API chưa bật thì hiện nút bật đúng API đó, chạy `gcloud services enable` trong tab terminal với nguyên câu lệnh trên nút, cạnh đó là link tới trang Google chỉ tới. Tên API và project đều lấy từ chính lời CLI; Aime không tự quyết anh cần API nào. Lỗi đó trước đây ập đến cả loạt project cùng lúc, và lý do đáng biết: Cloud Asset kiểm API đã bật hay chưa theo project bị TÍNH PHÍ lệnh đó, mà mặc định là project mà `gcloud config set project` trỏ tới gần nhất - không phải project đang xem - nên một lần đặt mặc định trong terminal của anh là đỏ hết mọi tab. Lệnh liệt kê của Aime tính phí theo lối cũ thay vì vậy, bằng `--billing-project LEGACY`, chính là giá trị CLI dùng để không gửi project tính phí nào; đo trên mười ba project, nên thứ panel hiện được không còn phụ thuộc vào một thiết lập thuộc về terminal của anh. Đặt một tài khoản làm mặc định của CLI (dấu ★ trong cột) phải xác nhận một bước, vì nó ghi vào config của CLI và đổi luôn thứ terminal của anh dùng. Supabase cũng đi cùng một đường nhưng theo hình dạng của nó: tài khoản là PROJECT nằm dưới organization sở hữu nó, đọc từ `supabase projects list`, và một project gồm database Postgres, các Edge Function và các preview branch - ba thứ tạo nên project, vì CLI của nó không có lệnh kiểm kê chung; lệnh đọc được kiểm theo cây lệnh của chính CLI thay vì `--help`, vì CLI này trả `--help` exit 0 kể cả với lệnh không tồn tại. Đăng nhập chạy `supabase login` trong tab terminal - CLI từ chối luồng trình duyệt khi không có terminal thật - và vì token nằm trong keyring của hệ chứ không phải file, panel hỏi lại CLI vài giây một lần trong ba phút xem đã đăng nhập chưa, rồi tự đọc danh sách project. Cuối cùng là radar - 'Ghi vào bộ nhớ AI' trên header tài khoản - nhờ AI đi tìm những gì ĐANG CHẠY trong tài khoản đó, chỉ đọc, và ghi kết quả vào AGENTS.md của dự án, file mà mọi AI CLI đọc mỗi lượt, để lần deploy hay lần săn bug tiếp theo bắt đầu từ đúng web app, database, queue thật của anh chứ không phải từ 'dự án kiểu này thường có gì'; mỗi lượt dò còn buộc phải nói rõ nó KHÔNG thấy được gì, thay đúng phần của mình mỗi lần, không đụng ghi chú của anh, và không ghi gì nếu câu trả lời về không đọc được.\n\nLàm được gì với cái này - tab thứ ba của mọi resource Google Cloud - là chỗ panel LÀM việc chứ không chỉ kể. AI được hỏi một lần cho mỗi LOẠI resource, sau không hỏi nữa: với loại này thì dev thường làm gì - xem log, restart, sửa biến môi trường, chuyển traffic, scale. Nó trả về câu lệnh viết theo placeholder và không bao giờ thấy resource thật, rồi từng câu đi qua đúng cái checker mà các bước deploy đi qua - từ lệnh phải có thật trong `gcloud --help` trên máy này, không được đụng vào tài khoản, không cờ nào được thay thế setting của service đang chạy. Xoá thì ở đây ĐƯỢC phép, khác với lúc deploy, vì xoá một resource là việc bình thường và từ chối nó chỉ đẩy anh ra console web làm đúng việc đó với ít rào hơn: Aime tự đọc câu lệnh để biết nó xoá - không tin vào lời AI gán nhãn - hiện nguyên lệnh ra, và nút vẫn chết cho tới khi anh gõ đúng tên của chính resource đó - nên việc hiện ra là việc Aime kiểm được, và số việc bị từ chối hiện ngay trên panel. Việc chỉ đọc thì bấm là chạy. Việc có ghi thì hiện câu lệnh trước, đã điền tên và region của resource, ghim đúng project và account do Aime thêm, giá trị sửa được ngay (chỗ `KEY=VALUE` của env var là của anh) và được kiểm lại sau khi sửa; chỉ khi bấm Chạy thì mới có gì xảy ra. Output về từng dòng ngay tại đó, có nút Dừng, và exit code ở cuối.\n\nDeploy - nút trên header của một project Google Cloud - deploy dự án đang mở lên đó, và được dựng sao cho AI không bao giờ cầm CLI cloud: đo được plan mode của Claude Code vẫn chạy lệnh, nên AI chỉ đọc repo bằng bộ tool chỉ-file và trả về KẾ HOẠCH, Aime kiểm rồi chạy. Bốn bước. Đầu tiên AI đọc repo và inventory của project, nói app là gì, resource nào đã là nó, và cần đọc gì thêm (`describe` service đang có); Aime chạy các lệnh đọc đó. Rồi AI viết kế hoạch: đích (service đang có, hoặc đề xuất kiến trúc kèm chi phí và hiệu năng khi chưa có gì), các setting hứa giữ nguyên, từng lệnh `gcloud`, file cần viết, và cách kiểm chứng - lệnh đọc trả về URL và mã HTTP mà GET vào health path phải trả. Aime kiểm từng lệnh trước khi anh thấy: từ lệnh phải tồn tại (`gcloud … --help` exit 0), không được xoá, gỡ hay đụng vào tài khoản - trừ đúng một ngoại lệ đã đo được, `gcloud projects add-iam-policy-binding`, vì build từ source trên project mới luôn hỏng cho tới khi service account build được cấp role, mà từ chối nó thì lại đẩy anh ra console web đúng ở lỗi hay gặp nhất; lệnh cấp quyền đó chỉ được THÊM binding, chỉ cấp cho một `serviceAccount:`, và không bao giờ được cấp `roles/owner`, `roles/editor` hay role quản trị IAM, và với service đang chạy thì không được dùng cờ `--set-*`, `--clear-*`, `--remove-*` vì chúng thay thế hoặc xoá sạch cái service đang có - `--update-*` mới là gộp thêm. Khi trang xác nhận hiện ra, chưa có gì chạy; nó hiện đúng từng lệnh sẽ chạy với project và tài khoản do Aime ghim, và nút Deploy là đường đi duy nhất. Sau đó Aime nhờ AI viết file, đọc các setting cần giữ, chạy từng bước với output trong log, bước lỗi thì đưa lại AI kèm output để viết lại phần còn lại (tối đa sáu vòng, cùng một lỗi hai lần là dừng), đọc lại các setting đã giữ và báo cái nào đổi, cuối cùng tự gọi HTTP vào URL vừa deploy - Xong nghĩa là service đã trả lời. Khi lần gọi đó trả về sai mã, Aime hỏi thêm chính gốc `/` của service, vì nhìn từ một request thì hai chuyện giống hệt nhau mà bản chất khác hẳn: đo trên một lần deploy thật, Cloud Run tự trả lời `/healthz` bằng trang 404 của Google và không hề chuyển vào container, trong khi mọi đường dẫn khác vẫn phục vụ - không có bước hỏi thêm này thì một lần deploy đã thành công lại bị báo là hỏng. Dừng kết thúc việc đang chạy; Về danh sách resource quay lại panel trong khi deploy vẫn tiếp tục.",
    keywords:
      "cloud azure aws gcp google supabase kết nối đăng nhập cli account subscription dò kiến trúc hạ tầng deploy devops bộ nhớ agents.md chỉ đọc bảo mật tab chọn tick ẩn đóng quota tính phí project mặc định cloud asset api chưa bật bigquery bq dataset",
  },
  {
    id: "mcp",
    section: "AI",
    title: "MCP server (plugin cho AI)",
    body: "Nút phích cắm trên header panel AI (hoặc 'MCP server' trong command palette) quản lý các MCP server của CLI đang chọn - issue tracker, tài liệu, database… để AI dùng như tool. Nút 'Duyệt & tìm server' chuyển hộp thoại sang danh mục có sẵn chia theo nhóm - issue board, thiết kế, tài liệu, cloud, tool chạy máy - kèm ô tìm kiếm trên registry MCP chính thức, nên server nào đã publish ở đó đều thêm được bằng một cú bấm. Chọn một mục sẽ điền sẵn vào form (kể cả biến môi trường nó cần) chứ không tự thêm sau lưng bạn; anh vẫn có thể tự dán URL hoặc gõ lệnh kiểu npx -y server-github. Aime gọi đúng lệnh mcp của CLI nên server nằm đúng chỗ CLI mong đợi, health check và OAuth của nó vẫn chạy - lưu ý Claude Code lưu theo dự án còn Codex lưu global; nút chìa khóa trên mỗi dòng mở terminal để đăng nhập server đó.",
    keywords: "mcp plugin tool server thêm xoá connector github notion",
  },
  {
    id: "git",
    section: "Git",
    title: "Panel Git",
    body: "Tab Git ở sidebar (hoặc bấm tên branch trên status bar) hiển thị thay đổi: stage/bỏ stage từng file hoặc tất cả, hủy thay đổi, commit (Ctrl+Enter trong ô nội dung), push và pull. Bấm vào file để xem diff so với HEAD. Khung diff đó nhảy thẳng tới thay đổi đầu tiên chứ không đứng ở dòng 1, tự cập nhật khi anh sửa tiếp file đó, và hiện cả thay đổi chỉ dịch khoảng trắng - loại mà một khung diff mặc định giấu đi. Nếu hai bên đúng là giống hệt nhau thì nó nói thẳng, thay vì để anh đi tìm một thay đổi không có trên màn hình: khi đó cái git liệt kê là thay đổi đã stage, thay đổi kiểu xuống dòng, hoặc đổi quyền file. Chính tab Git mang một huy hiệu ghi số file chưa commit, đổi sang đỏ khi có file xung đột, nên anh biết có gì đang chờ mà không cần rời tab đang xem. Khu Lịch sử (gập/mở, cuộn riêng, nút Tải thêm) liệt kê commit - bấm một commit để mở nó thành danh sách các file nó đụng tới, bấm một file để xem patch của đúng file đó. File bị đổi tên được hỏi bằng cả hai tên nên đọc ra đúng là đổi tên chứ không phải thêm mới nguyên file; commit merge được đọc theo nhánh mà anh merge vào, vì đó là cách duy nhất git chịu hiện thứ nó mang về; còn file mà git báo không có thay đổi theo dòng thì nói thẳng ra chứ không mở một editor trống. Thay đổi, Lịch sử và Stash là ba khu, mỗi khu một thanh chia: kéo một khu thì khu kia nhường, gập một khu thì nó chỉ còn một dòng. File mới được liệt kê từng file kể cả khi cả thư mục là mới, không có file nào nấp sau một dòng thư mục. File thay đổi có chữ trạng thái trong cây file, file bị git bỏ qua thì hiện mờ ở đó (cả thư mục bị ignore mờ một lượt, không phải liệt kê từng file bên trong), còn dòng thay đổi có vạch màu ở lề editor. Mọi thao tác đều nói là nó đang chạy: fetch, pull, push hiện spinner ngay trên nút anh vừa bấm, còn thứ khởi từ menu đã đóng - merge, chuyển nhánh, stash, tag, revert, reset - thì tự xưng tên trên một dòng dưới thanh nhánh. Trước đây chúng im lặng suốt mấy giây làm việc với remote, đọc ra thành app treo chứ không phải app đang chạy. Thông minh mặc định: commit khi chưa stage gì sẽ tự stage toàn bộ, push lần đầu của nhánh mới tự thiết lập upstream. Hoạt động hoàn toàn không cần AI; có AI thì nút lấp lánh tự viết nội dung commit từ đúng thứ commit tới sẽ mang: index nếu đã stage, còn không thì thay đổi trong thư mục làm việc - kể cả file git chưa từng thấy, vì `git diff` không hiện file mới nào cả, mà một thay đổi toàn file mới lại đúng là loại cần được mô tả nhất.",
    keywords: "commit stage diff push pull nhánh branch quản lý phiên bản init",
  },
  {
    id: "git-review",
    section: "Git",
    title: "Nhờ AI soát lại trước khi commit",
    body: "Nút 'Soát lại' cạnh nút commit gửi đúng phần anh sắp commit cho AI và liệt kê những gì nó thấy: chỗ có vấn đề đánh dấu ! đỏ, nhận xét nhỏ đánh dấu chấm, bấm vào là mở đúng file đó. Đây là ý kiến chứ không phải cửa chặn - không chặn commit, và bấm X là tắt. Chưa stage gì thì nó soát luôn thay đổi trong thư mục làm việc, vì đó chính là thứ anh sắp stage - kể cả file mới, thứ mà không lệnh `git diff` nào báo.",
    keywords: "review soát lại ai trước commit nhận xét vấn đề",
  },
  {
    id: "git-branches",
    section: "Git",
    title: "Nhánh, remote và tag",
    body: "Bấm tên nhánh để chuyển nhánh, tạo nhánh, đổi tên nhánh hiện tại, merge nhánh khác vào, hoặc xoá nhánh - xoá nhánh còn commit chưa merge thì hỏi lại lần nữa trước khi mất phần việc đó. Danh sách có đủ mọi nhánh chứ không chỉ nhánh local: dưới các nhánh local là những nhánh remote bạn chưa checkout - toàn bộ việc của team trong một repo vừa clone - đánh dấu bằng icon đám mây; chọn một nhánh như vậy là checkout nó thành nhánh local theo dõi nhánh remote đó. Nhánh remote nào đã có bản local thì chỉ hiện một lần, dưới tên local. Đổi tên, merge và xoá chỉ áp dụng cho nhánh local. Các hành động nằm trên đầu menu, danh sách nhánh nằm dưới, nên repo cả nghìn nhánh cũng không chôn mất chúng. Từ khoảng chục nhánh trở lên, menu mở kèm ô lọc: gõ một phần tên là danh sách hẹp lại theo cái khớp, Enter chọn cái khớp nhất - và danh sách dài hơn cửa sổ thì cuộn ngay trong menu chứ không tràn ra ngoài màn hình. Menu chỉ vẽ vài trăm mục đầu, bên dưới có dòng ghi còn bao nhiêu mục nữa: muốn tới chúng thì gõ để lọc - ô lọc tìm trên toàn bộ danh sách chứ không chỉ phần đang hiện. Nút ... cạnh Refresh quản lý remote (thêm mới, hoặc trỏ remote cũ sang URL khác) và tag (tạo tag thường hoặc annotated, xoá, push toàn bộ). Mũi tên tải xuống là fetch - cập nhật nhánh từ remote mà không đụng file của bạn; nhánh đồng nghiệp vừa push chưa thấy trong danh sách thì fetch trước.",
    keywords:
      "nhánh đổi tên xoá merge remote origin url tag fetch checkout theo dõi clone toàn bộ nhánh lọc tìm danh sách dài cuộn còn bao nhiêu",
  },
  {
    id: "git-commit-view",
    section: "Git",
    title: "Đọc một commit",
    body: "Bấm vào một commit trong Lịch sử là nó mở ra giữa cửa sổ, và những gì anh thấy đúng là cách người ta đọc một commit: tiêu đề, ai viết và viết lúc nào, phần nội dung của message, rồi danh sách các file nó đụng tới - mỗi file kèm chữ cái cho biết chuyện gì đã xảy ra (thêm, sửa, xoá, đổi tên) và bao nhiêu dòng đã đổi. Bấm vào một file là hiện diff của đúng file đó, không kèm file nào khác. Trước đây chỗ này đổ nguyên một lệnh `git show` vào một khung soạn thảo: một khối thống kê không bấm được gì, rồi diff của mọi file nối liền nhau - với commit đụng hai mươi file thì đó là một bức tường chữ mà anh phải cuộn mãi để tìm đúng file mình cần. File đổi tên được liệt kê theo tên mới, vì đó mới là cái anh muốn mở, và rê chuột vào sẽ thấy nó vốn tên gì. File nhị phân thì nói thẳng là nhị phân chứ không hiện một cái diff vô nghĩa. Diff của từng file chỉ được đọc khi anh mở nó, nên commit đụng cả trăm file vẫn mở ra tức thì.",
    keywords:
      "commit lịch sử danh sách file thay đổi diff patch đổi tên nhị phân thêm sửa xoá số dòng tác giả message",
  },
  {
    id: "git-history-ops",
    section: "Git",
    title: "Hoàn tác và di chuyển commit",
    body: "Chuột phải vào một commit trong Lịch sử: chép mã commit, revert nó (tạo commit mới đảo ngược - cách an toàn cho việc đã push), cherry-pick sang nhánh hiện tại, hoặc chuyển nhánh về đúng commit đó. Reset có 3 kiểu và hộp thoại nói rõ từng kiểu: giữ thay đổi ở vùng stage, giữ trong file nhưng chưa stage, hoặc bỏ sạch - kiểu cuối ghi thẳng là không hoàn tác được.",
    keywords: "revert hoàn tác cherry-pick reset soft mixed hard commit sha",
  },
  {
    id: "git-conflicts",
    section: "Git",
    title: "Giải quyết xung đột merge",
    body: "File bị xung đột hiện trong khu Xung đột merge màu đỏ - bấm Giải quyết để mở trình xử lý. Mỗi xung đột hiển thị Bên mình và Bên kia cạnh nhau (kèm gốc chung nếu có): giữ một bên, giữ cả hai, tự sửa kết quả, hoặc để AI hòa giải từng khối - hay toàn bộ. Lưu & đánh dấu đã giải sẽ ghi file và stage luôn. Không bao giờ mất nội dung: nút lưu bị khóa cho tới khi mọi xung đột đều có quyết định.",
    keywords: "merge conflict xung đột giải quyết ours theirs ai rebase pull",
  },
  {
    id: "git-blame-branch",
    section: "Git",
    title: "Blame & nhánh",
    body: "Dòng đang đặt con trỏ hiển thị ai sửa lần cuối và khi nào (chữ mờ cuối dòng, kiểu GitLens). Bấm tên nhánh trong panel Git để chuyển nhánh hoặc tạo nhánh mới.",
    keywords: "blame tác giả nhánh branch checkout chuyển tạo",
  },
  {
    id: "git-stash-amend",
    section: "Git",
    title: "Stash & amend",
    body: "Nút lưu trữ trên header Thay đổi sẽ stash toàn bộ (kể cả file chưa track, message tùy chọn). Danh sách stash bên dưới với Pop (áp dụng & xóa), Áp dụng (giữ lại), Xóa, nằm trong khu riêng: kéo thanh chia để cho nó rộng bao nhiêu tùy anh, hoặc gập tiêu đề lại thì nó chỉ tốn đúng một dòng. Ô Amend cạnh nút Commit ghi đè commit cuối - để trống nội dung thì giữ message cũ. Mở file đang xung đột trong editor sẽ thấy banner đỏ nhảy thẳng tới trình giải conflict.",
    keywords: "stash cất tạm amend sửa commit cuối wip",
  },
  {
    id: "git-multiselect",
    section: "Git",
    title: "Chọn nhiều thay đổi",
    body: "Trong danh sách Đã stage/Thay đổi: click chọn một file (và mở diff), Ctrl+click thêm/bớt file vào vùng chọn, Shift+click chọn cả dải. Thanh branch và ô commit đứng yên khi danh sách file cuộn bên dưới, và kéo thanh chia phía trên khu Lịch sử để chia lại chiều cao giữa hai khu; chỗ anh thả được nhớ lại. Chuột phải mở menu để stage, bỏ stage hoặc hủy thay đổi tất cả file đang chọn cùng lúc. Cũng menu đó thêm file chưa theo dõi vào .gitignore - mỗi file một dòng, neo đúng đường dẫn đó, ghi nối vào .gitignore của dự án mà không đụng những gì đã có. Chỉ hiện với file chưa theo dõi, vì ignore một file git đang theo dõi thì không có tác dụng gì.\n\nMenu chuột phải trong cây file cũng làm đúng việc đó, và đây là chỗ ignore được **cả thư mục** - việc mà danh sách Thay đổi không làm được vì nó chỉ liệt kê file. Thư mục được thêm dấu `/` cuối theo đúng quy ước git, nên sau này có file trùng tên thì vẫn được theo dõi bình thường. Điều kiện hiện mục này cũng vậy: chỉ với thứ chưa theo dõi (thư mục thì tính khi bên trong có file chưa theo dõi), và không bao giờ hiện với thứ đã bị ignore - cái đó đang xám trên màn hình rồi, thêm pattern nữa cũng vô nghĩa. File mà git đã theo dõi thì hiện mục khác: Ngừng theo dõi và bỏ qua - vì chỉ thêm pattern thì git đọc rồi bỏ qua. Aime hỏi trước và nói rõ cái giá: nó gỡ đường dẫn khỏi git rồi ghi pattern, file vẫn nằm nguyên trên máy anh, còn lần commit tới mới là lúc file biến mất với mọi người khác.",
    keywords:
      "chọn nhiều ctrl shift chuột phải menu hàng loạt gitignore thư mục cây file ngừng theo dõi untrack",
  },
  {
    id: "terminal",
    section: "Terminal",
    title: "Terminal tích hợp",
    body: "Ctrl+` (hoặc nút trên status bar) bật terminal PowerShell tại gốc dự án. Dấu + mở thêm tab; mỗi tab là một shell riêng, ẩn panel không làm mất phiên. Ctrl+V và Ctrl+Shift+V để dán; chuột phải sao chép phần đang bôi chọn, không bôi chọn thì dán, như Windows Terminal - đó là cách đưa mã xác minh vào một lệnh đăng nhập đang chạy trong đó.",
    keywords: "shell powershell tab console",
  },
  {
    id: "task-run",
    section: "Công việc",
    title: "Giao một việc rồi đi chỗ khác",
    body: "Nút ngôi sao trên một work item không còn gửi prompt vào chat nữa - nó khởi động một lượt làm việc. Một lượt gồm sáu bước - đúng sáu bước một developer vẫn làm - mỗi bước kết thúc ở một cổng mà thứ kiểm nó không phải là AI, vì một thay đổi chỉ đáng tin khi thứ phê duyệt nó không phải thứ đã viết nó. Sáu bước đó đúng như anh sẽ tự kể ra: hiểu task và đọc code liên quan, lên plan và chốt thế nào là chứng minh được, viết code và test của nó, test tới khi hết bug, tự soi lại việc mình làm, bàn giao - và mọi khẳng định đều phải đối chiếu với một thứ không phải tác giả. Trước khi tiêu một token nào, lượt chạy tự tạo nhánh riêng (không bao giờ dùng nhánh anh đang đứng), lưu một checkpoint để hoàn tác cả lượt bằng một cú bấm, rồi chạy *mọi* lệnh test và mọi lệnh kiểm mà dự án khai, để ghi lại cái gì đang xanh và cái gì đang sạch *trước khi sửa* - đó là thứ khiến mọi khẳng định sau này đo được chứ không phải nói cho vui. Dự án không khai script test nào cũng không bị bỏ cuộc: AI được hỏi dự án này thật ra test bằng gì - đọc từ cấu hình CI, Makefile hay docs, không được bịa - rồi Aime chạy thật từng lệnh nó nêu, chỉ giữ lệnh nào chạy được, và từ đó đo chúng như mọi suite đã khai. Sau đó nó đọc yêu cầu thành các tiêu chí nghiệm thu có số; chỗ nào yêu cầu không nói rõ thì nó chọn mặc định hợp lý và ghi vào báo cáo, thay vì đứng chờ một cái bàn không có ai ngồi. Nó đọc code ngay trong cùng pha đó, vì không đọc chỗ code sẽ nằm thì không chốt được yêu cầu là gì - những file sẽ sửa, và nếp nhà mà chính các file đó đang theo, mỗi nếp nhà phải chỉ ra đã thấy ở file nào - rồi hỏi language server xem còn file nào dùng tới thứ chúng khai, đúng câu hỏi mà 'Find all references' trả lời, nên danh sách là thật chứ không phải trí nhớ của model. File nào không server nào trả lời được thì báo là chưa biết, không bao giờ báo là an toàn. Nó cũng đọc luật mà dự án tự ghi ra - `AGENTS.md`, `CLAUDE.md`, `ARCHITECTURE.md`, `CONTRIBUTING.md`, file rule của Cursor hay Copilot - và mang theo suốt lúc chốt cách làm, lúc viết code và lúc tự soi. Luật đó đứng trên cả mặc định của AI và cả những gì nó tự suy ra từ code; Aime tự đọc chứ không phó cho việc CLI nào tình cờ nhặt được file nào. Rồi một trang duy nhất chốt toàn bộ hình dáng của việc: làm cách nào và vì sao - MỘT cách, cách tốt nhất nó thấy, vì đưa ra ba phương án bắt anh chọn là đẩy lại đúng cái quyết định anh đã giao - kèm phần một QC sẽ nhận ra ngay. Tiêu chí biến thành các test case có số: điều kiện đầu vào, thao tác, kết quả mong đợi, và **chứng minh bằng cách nào**. Chọn phép kiểm nào là việc của AI chứ không phải Aime đoán theo đường dẫn file, và case nào không nói sẽ kiểm bằng cách nào thì bị trả lại - một màn hình không thể được chứng minh bằng unit test của hàm phía sau nó. Test case ghi ra `.aime/test-cases.md` để đọc và `.aime/test-cases.json` để sửa; anh sửa file đó thì lượt chạy làm theo định nghĩa của anh, không phải của nó. Cùng trang đó định vị mọi case vào một test, thiếu một case là bị trả lại. Mặc định lượt chạy dừng ở đó với cách làm, test case và plan hiện trên màn hình mà chưa sửa file nào - lúc cuối cùng còn đổi hướng miễn phí; một công tắc trong panel cho nó chạy thẳng tới hết, khi anh đã tin nó với loại việc đó. Rồi code và test của nó được viết cùng nhau, đúng thứ tự đó, đúng cách anh vẫn làm - theo kiến trúc của dự án như một luật chứ không phải lời khuyên, và chỗ nào đụng tới màn hình thì phải giống phần còn lại của app: component của anh, token của anh, khoảng cách của anh, không phải một cái look riêng. Hiệu năng và bảo mật được coi là phần của tính đúng chứ không phải ghi chú review về sau, và OWASP Top Ten được gọi tên trong yêu cầu giao cho AI chứ không để tuỳ cảm nhận. Lượt chạy nào thoát ra mà không sửa file nào thì bị chặn và nói rõ; lượt nào không thấy file test mà plan đã hứa cũng vậy - plan hứa mỗi case một test, nên thiếu một file là một case không ai viết gì cho nó, và nó bị đòi lại đích danh. Cái giá của thứ tự này là mất đúng một phép kiểm - test viết rỗng vẫn xanh y như test làm việc thật - nên việc bắt nó được giao cho người soi ở bước sau: người soi đọc diff, và được yêu cầu coi một test mà xoá tính năng đi vẫn xanh là một phát hiện. Sau đó một pha đo tất cả và tự sửa chỗ nó làm vỡ, vì một lượt chạy báo thiệt hại rồi đi là giao lại bài tập chứ không phải giao việc. Cổng rẻ chạy trước - cổng kiểm của chính dự án: linter, type checker, formatter, sửa và chạy lại tới khi xanh, không bao giờ được tắt luật để đi qua - rồi mọi suite so với mốc nền: test đang xanh mà giờ đỏ thì quay lại thẳng cho AI kèm output làm bằng và đo lại, tới ba vòng; test đã đỏ sẵn thì báo chứ không đổ tội cho thay đổi này. Vòng sửa chỉ chạy lại đúng những suite đang đỏ, và khi chúng xanh thì toàn bộ chạy lại một lượt nữa - đó là thứ giúp một bản sửa hai dòng không phải trả tiền cho suite browser chín phút của anh ba lần. Chỉ thứ sửa không nổi mới chặn lượt chạy. Kế đó một người soi với context sạch tự dẫn ra rủi ro của *đúng thay đổi này* chứ không đi theo checklist, và lần nào cũng soi bốn thứ: kiến trúc của dự án, OWASP Top Ten ở chỗ áp được, hiệu năng, và màn hình vừa sửa có giống phần còn lại của app hay không. Những gì người soi tìm ra thì được sửa ngay trong bước đó, rồi đo lại tất cả - vì một bản review không ai xử lý là một tài liệu, không phải một cổng. Có hai loại phát hiện, nếu sửa không nổi, sẽ chặn hẳn lượt chạy: kiến trúc và bảo mật. Code nằm sai tầng thì không thuộc về repo của anh dù nó chạy tốt cỡ nào, còn một lỗ hổng kẻ tấn công dùng được thì không phải chuyện gu - trước đây hai loại này chỉ được ghi vào báo cáo rồi lượt chạy vẫn báo xong, tức là câu 'theo kiến trúc của dự án' âm thầm bị hạ xuống thành lời khuyên. Còn lại thì báo để anh phán, vì người soi hoàn toàn có thể sai. Cũng chính bước đó kết thúc theo đúng nghĩa một đội gọi là xong: build những gì dự án khai (build vỡ vì thay đổi này thì trả lại AI kèm output, tối đa ba vòng), deploy kết quả theo đúng cách dự án deploy - file CI, Dockerfile hay script của chính nó; không khai gì thì deploy tại chỗ, và không bao giờ bịa credential - smoke-test thứ vừa lên, rồi tự lái phần mềm ĐANG CHẠY qua từng test case đã chốt, tự cài thứ nó thiếu (headless browser, driver) và chỉ hiện tiến trình chứ không hỏi xin phép. Thứ nó để lại là mỗi case một bằng chứng trong `.aime/evidence/`, đặt tên theo case - ảnh chụp, response bắt được, output của chính runner - cộng bằng chứng trong `.aime/evidence/deploy/` rằng thứ vừa deploy có trả lời. Aime không tin lời khai nào: từng file phải tồn tại, khác rỗng và mới hơn lượt chạy; case không có bằng chứng thì ghi 'chưa chứng minh', còn lượt không có bằng chứng phần mềm chạy thì bị chặn - vì chưa bàn giao được gì. Toàn bộ phần đó được bỏ qua với thay đổi mà chính lượt chạy đã xác định là không cần deploy để chứng minh - một hằng số đổi tên thì không có phần mềm nào đang chạy để mà chụp, đòi bằng chứng cho nó thì hoặc chặn oan, hoặc bị làm hài lòng bằng một file cho có, mà cái thứ hai còn tệ hơn. Việc xác định đó là của AI, nêu ngay trên trang anh duyệt, dựa trên bộ case nó vừa viết: nó đọc project còn Aime thì không. Thứ anh quay lại nhận là một nhánh, một bảng mọi test case đối chiếu với việc nó có thật sự được chứng minh hay không - và 'được chứng minh' vẫn phải xứng đáng: có test nêu đúng case đó trong cây, mọi suite trả lời đều xanh, và - với thay đổi buộc phải deploy mới tin được - có bằng chứng từ phần mềm đang chạy mang tên case đó; dưới mức đó là ghi 'chưa chứng minh' kèm đúng điều kiện còn thiếu ngay trong ô, và báo cáo ghi rõ lượt này đã áp bộ điều kiện nào - kèm bằng chứng các suite để lại trên đĩa (ảnh chụp, trace, report), một báo cáo đầy đủ trong `.aime/runs/`, và một nút hoàn tác tất cả. Mọi lượt đều được lưu: 'Các lượt AI làm việc' trong command palette liệt kê những gì dự án này đã giao cho AI, mở lại được bất kỳ lượt nào, và mời anh chạy tiếp lượt bị cắt giữa pha - đóng máy chỉ mất đúng pha đó, không mất gì trước nó. Ngay cạnh lịch sử đó, một lượt đã xong có thêm khu dọn dẹp: một cú bấm liệt kê những gì lượt đó để lại - chỉ file do chính nó tạo ra, phân biệt bằng cách so danh sách untracked của git trước lượt chạy với bây giờ, cộng các thư mục artifact - và cú bấm thứ hai xoá đúng những gì anh tick. Bằng chứng mặc định không tick vì nó chứng minh công việc; test case, nhật ký và báo cáo không bao giờ bị đưa vào; file đã theo dõi hay thứ có sẵn từ trước thì không bao giờ xuất hiện trong danh sách. Hiện Aime đọc được tên test hỏng của cargo, vitest và dotnet test - cái cuối phủ cả xunit, MSTest và NUnit vì cả ba in qua cùng một runner; với runner khác thì các cổng vẫn làm việc bằng exit code, và nói thẳng là chỉ biết tới đó. Và các lượt chạy được song song: giao việc thứ hai khi việc thứ nhất còn đang chạy thì nó cứ thế bắt đầu, trong một git worktree riêng - hai lượt không bao giờ được dùng chung một checkout - được cài dependency bằng đúng lệnh install của dự án, và cuối lượt thay đổi được commit lên nhánh của chính nó (worktree chỉ là chỗ làm, nhánh mới là thứ bàn giao), còn lượt thứ nhất vẫn giữ cây làm việc của anh y như trước. Panel mọc thêm tab cho mỗi lượt đang sống, mỗi tab có log, cổng và báo cáo riêng; nút dọn dẹp của lượt worktree còn liệt kê cả chính worktree đó, mặc định giữ vì bằng chứng nằm bên trong.",
    keywords:
      "lượt làm việc task run agent tự động sáu bước pha cổng mốc nền regression tiêu chí nghiệm thu test case QC điều kiện thao tác kết quả chứng minh bằng cách nào phương án quyết định plan implement đo lại cổng kiểm lint review deliver build deploy smoke owasp bảo mật kiến trúc luật dự án AGENTS.md CLAUDE.md style ui nhánh checkpoint hoàn tác phạm vi ảnh hưởng bằng chứng ảnh chụp artifact lịch sử chạy tiếp báo cáo dọn dẹp dọn rác xoá untracked makefile gradle ci cà phê không cần trông",
  },
  {
    id: "work-items",
    section: "Công việc",
    title: "Kết nối bảng công việc (Azure DevOps, Jira, ClickUp, GitHub)",
    body: "Tab dấu tích trên sidebar (hoặc 'Bảng công việc' trong command palette) hiện việc được giao cho bạn trên bảng, và nối nó với repo đang mở. Lúc kết nối chỉ hỏi đúng những gì dịch vụ cần, và form được dựng từ đó: Azure DevOps cần tổ chức, dự án và một personal access token có quyền đọc + cập nhật work item (Azure DevOps Server on-premises thì thêm địa chỉ server của nó; dự án có nhiều board thì ghi tên team của board anh muốn, danh sách sẽ đúng bằng những gì board đó chứa - vì chính team là thứ định nghĩa một board, nên Aime đi hỏi team chứ không tự đặt ra bộ lọc). Nếu anh làm nhiều dự án trong cùng một tổ chức thì ghi hết vào ô đó, cách nhau bằng dấu phẩy: một token, một connection, một danh sách, có thêm nhóm 'Project' để gom - và mỗi việc vẫn mở, đổi trạng thái, nhận comment đúng trong dự án của chính nó; Jira có hai mục vì nó là hai sản phẩm: Jira Cloud cần địa chỉ site, email tài khoản Atlassian và một API token; Jira Server / Data Center cần URL nội bộ của công ty và một personal access token, không cần email - cả hai đều để dự án không bắt buộc, bỏ trống là thấy việc của anh trên toàn site; ClickUp chỉ cần API token, và tên workspace nếu tài khoản anh có nhiều hơn một; GitHub cần personal access token, thêm repository nếu muốn giới hạn trong một repo, và địa chỉ API chỉ khi GitHub của anh là Enterprise Server nội bộ. GitHub liệt kê issue (không bao giờ liệt kê pull request) với đúng hai trạng thái mà issue có, và lúc đóng thì hỏi rõ đóng kiểu nào - đã xong, hay sẽ không làm. Aime chỉ lưu sau khi một truy vấn thật đã trả về, nên 'đã kết nối' nghĩa là nó chạy được. Bảng gắn theo project đang mở: kết nối là gắn thư mục này vào bảng đó, mở project khác thì thấy bảng của chính project đó, hoặc được hỏi nó dùng bảng nào - chỉ một cú bấm vì token đã có sẵn. Còn token là của anh chứ không của repo, và không bao giờ được ghi vào trong repo. Danh sách tự xếp theo cách bảng của anh được tổ chức chứ không cố định một kiểu: việc rải trên nhiều board hoặc nhiều sprint thì nhóm theo board / sprint, việc phần lớn nằm dưới một thứ lớn hơn thì nhóm theo thứ nó thuộc về (user story, feature, test suite), còn một danh sách phẳng toàn việc độc lập thì nhóm theo chỗ nó đang đứng - đang làm, chờ làm, đã xong. Nút gạt cạnh 'Của tôi / Tất cả' đổi cách xếp khi Aime đoán không đúng ý, và giữ luôn thứ duy nhất còn lại mà anh chỉ chỉnh một lần: có tính việc đã xong hay không. Nhóm của chính dự án đang mở còn được đưa lên đầu, gắn nhãn 'dự án này' và mở sẵn - Aime suy ra từ chính repo chứ không bắt anh khai báo: một nhánh trong repo đặt tên theo item của nhóm đó (nhánh đang checkout được tính nặng nhất, vì đó mới là việc anh đang làm), hoặc một board trùng tên với thư mục / remote của repo. Khi có nhiều board mà phần còn lại cộng lại dài hơn một màn hình thì chúng gấp sẵn, vẫn hiện số lượng, mở lại chỉ một cú bấm; còn anh tự gấp một nhóm thì Aime thôi không quyết nữa. Mỗi tiêu đề nhóm gấp lại được, mang theo số lượng, và hiện tám dòng đầu - phần còn lại cách một cú bấm; đó là thứ làm một bảng bốn mươi việc trở nên đọc được. Mỗi dòng giữ nguyên chữ của nhóm bạn cho loại và trạng thái, vì tên trạng thái thuộc process template của bạn chứ không phải của Aime - còn màu nền của trạng thái là của Aime, và chỉ nói đúng thứ mọi bảng đều đồng ý: đang làm, chờ làm, hay đã xong. Tiêu đề được hai dòng chứ không phải một, vì tiêu đề là phần duy nhất anh không thể đoán; và loại việc bị bỏ đi khi cả bảng chỉ có một loại (GitHub gọi mọi thứ là 'Issue'), vì một chữ lặp trên mọi dòng chỉ là một cột nhiễu. Việc mà anh đang đứng trên nhánh của nó được đánh dấu ở lề và nằm đầu nhóm: trong tất cả những gì được giao, đó là việc anh đang làm ngay lúc này, không đời nào bắt anh cuộn đi tìm. Những gì anh đã dựng - ô lọc, cách nhóm, các nhóm đã gấp - vẫn còn nguyên khi anh ngó sang tab khác của sidebar rồi quay lại, và chỉ đặt lại khi mở project khác. Bấm vào một dòng là mở việc đó ở giữa cửa sổ, đúng chỗ một tab editor, vì đó mới là chỗ đủ rộng để đọc: nó nằm dưới cái gì, nội dung được dựng lại từ Markdown mà mọi connector đều trả về (tiêu đề vẫn là tiêu đề, các bước vẫn là danh sách đánh số, code vẫn nằm trong khối, link mở bằng trình duyệt), các thông tin ngắn mà dịch vụ giữ (giao cho ai, ưu tiên, còn bao nhiêu việc) và cả hội thoại - mọi comment, kèm ô để nói thêm một câu, ô đó chỉ trống khi bảng đã nhận thật. Trạng thái cạnh tiêu đề là một menu, nên đổi trạng thái được ngay tại chỗ đang đọc. Phía trên danh sách, 'Của tôi' và 'Tất cả' quyết định hỏi bảng về việc của ai - mặc định là của bạn - còn ô lọc thì thu hẹp ngay những gì đã tải về (theo tiêu đề hoặc id, không cần dấu: gõ 'dang nhap' vẫn tìm ra 'đăng nhập'), nên nó trả lời nhanh như bạn gõ. Việc đã xong bị ẩn cho tới khi bạn bật 'Hiện cả việc đã xong' trong chính menu tuỳ chọn đó, và mọi dịch vụ đều được đọc tới cùng một mức: 300 việc anh vừa động tới gần nhất. Một người mà được giao nhiều hơn thế thì đó là chuyện sắp xếp việc, không phải chuyện hiển thị. Mỗi dòng gọi tên việc bằng đúng id anh đọc ra miệng - custom id kiểu DEV-123 của ClickUp chứ không phải id nội bộ mà API dùng - và tên nhánh cũng lấy theo id đó. Trên mỗi dòng: mở trong trình duyệt, đổi trạng thái (danh sách đọc từ chính dịch vụ, không đoán - với Jira là đúng những transition mà issue này đi được từ chỗ nó đang đứng, với ClickUp là các status mà list của task đó định nghĩa), tạo nhánh với tên gợi ý từ chính work item và sửa được trước khi tạo, hoặc đưa việc đó cho AI - mô tả, tiêu chí nghiệm thu và các bước tái hiện lỗi được đưa vào prompt dưới dạng Markdown, đúng dạng mà một agent đọc tốt nhất. Token hết hạn thì panel nói đúng một dòng, kèm nút 'Kết nối lại' giữ nguyên dự án bạn đã gõ. Nút '…' trên header giữ những thao tác với chính cái bảng chứ không phải với danh sách: chuyển project này sang một bảng khác đã kết nối, cho project này thôi dùng bảng mà không đụng tới bảng, hoặc xoá bảng ở mọi nơi kèm token - cái cuối có hỏi trước khi làm. Token nằm trong thư mục config của Aime trên máy này, và chỉ được gửi tới đúng dịch vụ của bạn. Muốn chính AI truy vấn bảng thì thêm mục tương ứng trong danh mục MCP - Azure DevOps (tự mang sẵn tổ chức của bảng anh đã kết nối) hoặc Atlassian cho Jira.",
    keywords:
      "azure devops bảng công việc work item task backlog sprint milestone giai đoạn pat token nhánh trạng thái comment bình luận giao cho tôi lọc tìm nhóm cha user story test suite team dự án này liên quan repo remote",
  },
  {
    id: "tasks",
    section: "Task",
    title: "Chạy, build và test",
    body: "Nút play trên thanh trạng thái (hoặc 'Chạy task' trong command palette) liệt kê năm mục tiêu - chạy, build, test, kiểm tra, publish - chứ không chỉ nhứng lệnh Aime tình cờ nhận ra. Stack phổ thông thì Aime tự biết và biết ngay: script trong package.json, cargo, go, dotnet, pytest, docker. Còn lại - Maven, Gradle, CMake, Composer, một script team anh tự viết - thì mục tiêu vẫn đúng là thứ anh muốn, nên việc đọc dự án là một BƯỚC BÊN TRONG mục đó, không phải một cái nút thứ hai anh phải biết tới. Nó chạy ngay khi mở dự án, trong nền: AI đọc repo - manifest, cấu hình CI, Makefile, README - rồi báo lệnh cho từng mục tiêu, kèm tên file nó đọc được, nên một lệnh `mvn package` nghe hợp lý cho repo không có pom.xml không lọt qua được. Không tin lời AI chệ: Aime xác nhận tool có trên máy và thư mục có trong repo, nói rõ lệnh nào bị bỏ và vì sao, rồi mới giữ phần còn lại. Phần sống sót được ghi vào .aime/tasks.json, nên mỗi repo chỉ trả giá đọc một lần và sau đó anh sửa tay được. Gốc repo mà chỉ nói cách ĐÓNG GÓI thứ bên dưới - Dockerfile nằm cạnh thư mục src/ - thì thư mục con cũng được dò, vì Dockerfile không nói gì về chỗ source nằm đâu. Solution .NET được đề nghị build, test, publish nhưng không có chạy: `dotnet run` nhận project chứ không nhận solution, và chọn project nào trong hai mươi cái là việc của AI, không phải đoán theo tên file. Mỗi script test là một task riêng - repo nào tách `test` với `test:e2e` là có hai suite, và một lượt AI làm việc đo cả hai chứ không đo một nửa dự án - còn script không bao giờ tự thoát (`test:watch`, `test:ui`, `test:debug`) thì được đề nghị như thứ để bật lên xem, vì cổng nào chạy nó cũng chỉ ngồi hết thời gian chờ mà không biết thêm gì. Các lệnh dự án tự kiểm mình cũng được liệt kê: `check`, `lint`, `typecheck`, `format:check`, và `cargo clippy` với `cargo fmt --check` cho crate Rust - chỉ những lệnh chỉ-đọc, vì `format` và mọi thứ `:fix` sẽ ghi lại chính đoạn code chúng đang chấm. Script được chạy bằng manager mà chính dự án khai - npm, pnpm, yarn hay bun, lấy từ field packageManager trước rồi mới tới lockfile - nên một dự án pnpm không bao giờ bị đưa lệnh npm. Workspace mà gốc không khai script build/test riêng thì được đề nghị lệnh chạy hết mọi thành viên. Còn repo mà gốc không build gì - kiểu frontend nằm cạnh api - thì từng thư mục được dò riêng, hiện là 'frontend · npm test' và chạy đúng trong thư mục đó. Mỗi task chạy trong tab terminal riêng mang tên nó, nên anh thấy output thật và tương tác được. Thêm hoặc ghi đè task trong .aime/tasks.json (id, label, kind, command, và cwd cho thư mục con) - entry nào dùng lại id đã dò được thì thay thế cái đó.",
    keywords:
      "task chạy build test kiểm lint typecheck clippy publish npm pnpm yarn bun workspace monorepo cargo dotnet go pytest docker tasks.json e2e watch",
  },
  {
    id: "tasks-fix",
    section: "Task",
    title: "Nhờ AI sửa khi task lỗi",
    body: "Khi task kết thúc với mã lỗi khác 0, một thanh đỏ hiện phía trên terminal. Nút 'Nhờ AI sửa' gửi lệnh, mã lỗi và output đã bắt được sang panel AI, yêu cầu AI tìm nguyên nhân, sửa rồi chạy lại task để xác nhận. Không dùng AI thì thanh đó chỉ là thông báo lỗi, bấm X để bỏ qua.",
    keywords: "sửa lỗi build fail exit code ai khắc phục",
  },
  {
    id: "ai-setup",
    section: "Editor",
    title: "Mở ngôn ngữ Aime chưa hỗ trợ sẵn",
    body: "Một thanh hiện lên đầu editor khi file đang mở thiếu thứ mà cài vào máy này là xong: thiếu language server (không có gợi ý, không xem được kiểu), hoặc thiếu đúng trình gỡ lỗi Aime dùng cho ngôn ngữ đó - debugpy cho Python, delve cho Go. Thanh này cũng hiện khi language server đã cài mà không khởi động được - editor im lặng y hệt, nhưng cách sửa khác hẳn: thanh đưa cho AI đúng thông báo lỗi của server để nó sửa môi trường quanh server (thiếu runtime, hỏng dependency) thay vì cài lại một gói vốn đã có sẵn. Ngoài hai trường hợp đó thanh này im, và đó là chủ ý. File Markdown, JSON hay CSS không có chương trình nào để bước từng dòng nên không bao giờ bị mời cài trình gỡ lỗi; trình gỡ lỗi nào Aime tự tải được thì chỉ là một cú bấm trong 'Chạy và gỡ lỗi'; còn ngôn ngữ Aime chưa có adapter thì phải chờ bản Aime mới chứ cài gì cũng không chạy - mời cài lúc đó là hứa điều editor không giữ được. Nút 'Để AI lo' giao đúng khoảng thiếu đó cho AI CLI của anh - kèm chính xác lệnh Aime đang dò, adapter Aime sẽ chạy, và bản thân file - rồi agent đọc dự án, tự tìm ra toolchain của máy này cần gì, cài, và in version từng tool ra để chứng minh. Nó chạy trong hộp thoại tiến trình riêng - không phải khung chat, nên không chiếm phiên hội thoại anh đang làm - theo đúng mức quyền anh đặt, và sẽ dừng lại hỏi chứ không tự vượt qua chuyện licence, tải vài GB hay hộp thoại quản trị. Có lượt cài đo được chạy hơn mười phút, nên hộp thoại đó có nút Huỷ, và đóng nó lại thì việc vẫn chạy ngầm. Chạy xong thành công thì Aime tự dò lại thứ nó còn thiếu, chip trạng thái xanh luôn mà không cần khởi động lại. Nút 'Cài ngay' vẫn nằm cạnh cho trường hợp Aime đã biết đúng một lệnh - cách đó nhanh hơn và không tốn token.",
    keywords: "chưa hỗ trợ ngôn ngữ cài đặt ai tự động toolchain thiếu server gỡ lỗi",
  },
  {
    id: "debug",
    section: "Gỡ lỗi",
    title: "Chạy chương trình với điểm dừng",
    body: "Mở file muốn chạy rồi nhấn F5, hoặc vào tab con bọ ở sidebar (Chạy và gỡ lỗi). Bấm vào lề bên trái số dòng - hoặc nhấn F9 - để đặt điểm dừng; F5 chạy chương trình và nó sẽ dừng ở đó. Thanh công cụ trên đầu editor để bước: F10 qua một dòng, F11 vào trong lời gọi, Shift+F11 ra ngoài, F5 chạy tiếp, Shift+F5 dừng hẳn. Khi chương trình đang dừng, sidebar hiện ngăn xếp lời gọi và biến của khung đang chọn; bấm vào một khung sẽ mở đúng file của nó. Điểm dừng chỉ đặt được trong file Aime chạy được thật - ngôn ngữ mà Aime có adapter - nên README hay package.json không nhận điểm dừng nào, và F5 trên file không gỡ lỗi được sẽ mở 'Chạy và gỡ lỗi' kèm lý do chứ không im lặng.\n\nF5 chạy chương trình của dự án, không phải file đang mở. Aime đọc manifest của dự án để biết chương trình đó - package Go, 'main'/'bin' trong package.json, manage.py hay main.py, project .NET dạng thực thi - và nút bấm ghi đúng cái nó sẽ chạy. Khi một repo có nhiều chương trình (service Go nằm cạnh front end Node), chính file anh đang sửa chọn thay anh: mở file nào dưới services/api thì target là api kèm đúng tham số của nó, chuyển sang services/worker thì cả hai đổi theo - không phải mở picker. Dòng 'Chạy' vẫn cho chọn tay và lựa chọn đó được nhớ theo dự án; chỉ file nằm trong một chương trình khác mới lật được nó, còn module dùng chung hay README thì không đụng tới. Chữ 'tạm đoán' là lúc Aime phải đoán vì file đang mở không thuộc chương trình nào. Dự án mà Aime không đọc ra entry point thì quay về chạy file đang mở - cũng là cách gỡ lỗi một script lẻ, chọn nó ở 'Chạy' là nó được nhớ luôn.\n\nĐiểm dừng không nhất thiết phải dừng mọi lần. Shift+F9 - hoặc cái bút chì cạnh nó trong danh sách điểm dừng - mở ba ô: chỉ dừng khi một biểu thức đúng, dừng sau bao nhiêu lần trúng (theo cú pháp của chính adapter: 5, > 5, % 10), hoặc ghi một dòng log rồi chạy tiếp - tức logpoint. Trình gỡ lỗi tự tính cả ba, nên điều kiện sai thì không tốn gì. Điểm dừng có điều kiện được vẽ rỗng, trỏ chuột vào là thấy điều kiện.\n\nMục 'Dừng khi có exception' liệt kê đúng những gì trình gỡ lỗi cho phép - js-debug có caught và uncaught, debugpy thêm raised và user-uncaught - và lần đầu thì áp đúng mặc định của nó. Aime nhớ danh sách đó nên các ô tick vẫn còn giữa các lần chạy, và tick vào là tới ngay chương trình đang chạy.\n\nMục 'Theo dõi' giữ biểu thức trên màn hình: chúng được tính trong khung anh đang chọn, nên chọn khung khác là tính lại, và biểu thức lỗi thì giữ nguyên lời của trình gỡ lỗi chứ không biến mất. Nút 'Tham số' mở phần chương trình được truyền vào - một dòng lệnh, giữ nguyên cụm trong ngoặc kép, và mỗi dòng một KEY=VALUE cho biến môi trường - lưu theo từng chương trình trong .aime/launch.json nên mai chạy vẫn y như hôm nay. 'Gắn vào chương trình đang chạy' thì với tới thứ đang chạy sẵn: đúng cổng mà `node --inspect` hoặc `python -m debugpy --listen` in ra. Dấu × cạnh ô cổng đóng khung đó lại, Escape cũng vậy.",
    keywords: "gỡ lỗi debug điểm dừng breakpoint bước f5 f9 f10 f11 ngăn xếp biến",
  },
  {
    id: "debug-adapters",
    section: "Gỡ lỗi",
    title: "Aime gỡ lỗi được những gì",
    body: "JavaScript và TypeScript qua js-debug, Python qua debugpy, Go qua delve, C# và F# qua netcoredbg. Không thứ nào được đóng gói sẵn: js-debug và netcoredbg không có package để cài nên Aime tải bản đã ghim về thư mục dữ liệu của nó khi anh bấm 'Tải về' - còn debugpy chạy từ Python của chính anh và delve từ Go toolchain của anh, thiếu cái nào thì panel hiện đúng lệnh cài cái đó. delve vẫn tìm được dù GOPATH/bin không nằm trên PATH, vì `go install` mới là cách nó được cài. C# là ngôn ngữ duy nhất mà bấm chạy sẽ build trước: trình gỡ lỗi CLR gắn vào assembly chứ không gắn vào file .cs, nên Aime build dự án rồi gỡ lỗi thứ vừa build ra - trong lúc đó console ghi 'Building…'. C++ và Java cố tình chưa có cho tới khi adapter của chúng được chạy thật; ngôn ngữ nằm trong danh sách này là ngôn ngữ đã kiểm chứng, không phải đọc tài liệu rồi khai.\n\nNgôn ngữ không nằm trong danh sách đó vẫn gỡ lỗi được, vì Aime học được. Nút 'Để AI lo' giao cho agent đúng hợp đồng: cài adapter mà ngôn ngữ đó dùng rồi ghi `.aime/debug-adapters.json` - cách khởi động, nói qua pipe hay socket, các field mà launch request cần, bước biên dịch nếu có, kèm một chương trình và một dòng để kiểm chứng. Rồi Aime tự kiểm: nó khởi động adapter, đặt breakpoint ở dòng đó, chạy, và chỉ tính là gỡ lỗi được nếu chương trình dừng thật ở đó. Chưa kiểm thì panel 'Chạy và gỡ lỗi' nói thẳng và mời kiểm chứng chứ không cho chạy - kiểm thất bại thì anh nhận đúng lời của adapter, kèm nút giao lại cho AI sửa. Không có gì agent viết mà tự đánh dấu là đã kiểm chứng; Aime đóng dấu đó, sau khi chính nó thấy chương trình dừng. File của dự án thắng file của máy, và cả hai đều thua adapter dựng sẵn. Lời mời nằm ngay trong 'Chạy và gỡ lỗi': mở file của ngôn ngữ chương trình mà Aime chưa có adapter - php, ruby, lua - thì nút 'Để AI lo' nằm ngay dưới lời giải thích; còn file tài liệu như Markdown, JSON hay CSS thì vẫn chỉ có câu nói thẳng, không mời gì.",
    keywords:
      "js-debug debugpy delve netcoredbg go python node typescript csharp dotnet adapter tải cài build",
  },
  {
    id: "debug-console",
    section: "Gỡ lỗi",
    title: "Bảng điều khiển gỡ lỗi",
    body: "Biểu tượng con bọ bên trái panel dưới hiện những gì chương trình in ra - stdout chữ thường, stderr màu đỏ - và mã kết thúc khi chạy xong. Output được nối liền theo lúc nó tới chứ không tách thành từng dòng, vì trình gỡ lỗi cắt output ở đúng chỗ đường ống đẩy dữ liệu: một câu lệnh in có thể tới editor thành ba mảnh. Khi chương trình đang dừng, dòng dưới cùng cho phép tính một biểu thức trong khung đang chọn - một biến, một lời gọi, bất cứ gì ngôn ngữ đó cho phép - và phím ↑ lấy lại các lệnh đã gõ.",
    keywords: "console output stdout stderr mã lỗi in biểu thức repl evaluate",
  },
  {
    id: "debug-breakpoint-moves",
    section: "Gỡ lỗi",
    title: "Vì sao điểm dừng bị dời chỗ",
    body: "Đặt điểm dừng ở dòng trống hay dấu ngoặc đóng thì dấu chấm nhảy sang dòng khác. Đó là trình gỡ lỗi trả lời: nó chỉ dừng được ở dòng có lệnh chạy, nên báo về dòng gần nhất, và Aime vẽ dấu ở nơi chương trình thật sự dừng chứ không phải nơi anh bấm. Không phải trình nào cũng dời - delve từ chối thẳng dòng đó và nói lý do, nên dấu chấm chuyển màu vàng và trỏ chuột vào sẽ thấy lý do. Điểm dừng cũng đi theo chữ - gõ thêm phía trên thì nó dịch xuống cùng dòng của nó - và được nhớ theo từng dự án giữa các lần mở app.",
    keywords: "điểm dừng dời dòng verified sai chỗ ghi nhớ màu vàng từ chối",
  },
  {
    id: "error-log",
    section: "Giao diện & phím tắt",
    title: "Khi có gì đó hỏng",
    body: "Lỗi được ghi vào một file log ngay trên máy anh - lỗi render mà bình thường chỉ để lại màn hình trắng, promise bị bỏ rơi không ai bắt - và trong Cài đặt có nút mở nó ra. Không có gì được gửi đi đâu. Làm crash reporter thì nhàn hơn, nhưng stack trace mang theo đường dẫn file, tên dự án, đôi khi cả prompt; một editor mở repo riêng tư của người ta thì không có quyền đẩy mấy thứ đó cho bên thứ ba. Muốn báo lỗi thì file log nằm sẵn đó để anh đính kèm - theo quyết định của anh, không phải mặc định.",
    keywords: "nhật ký lỗi crash báo lỗi chẩn đoán riêng tư màn hình trắng",
  },
  {
    id: "updates",
    section: "Giao diện & phím tắt",
    title: "Cập nhật và kênh phát hành",
    body: "Aime kiểm tra đúng một lần mỗi lần mở, không hỏi lại trong phiên đó, và không tải gì cho tới khi anh bấm Cài - một editor cắt ngang lúc anh đang gõ là tự cho mình quan trọng quá mức. Trong Cài đặt có hai kênh: Ổn định chỉ nhận bản phát hành chính thức, còn Thử nghiệm nhận thêm bản beta - mới hơn và tương ứng là ít được kiểm chứng hơn. Mọi bản build đều được ký bằng khoá riêng của dự án và chữ ký được kiểm trước khi cài, kênh nào cũng vậy.",
    keywords: "cập nhật phiên bản beta kênh ổn định cài chữ ký",
  },
  {
    id: "settings",
    section: "Giao diện & phím tắt",
    title: "Cài đặt",
    body: "Ctrl+, nút bánh răng trên status bar, hoặc 'Cài đặt' trong palette mở một trang gom hết mọi thứ chỉnh được: theme, ngôn ngữ, cỡ chữ, xuống dòng tự động, độ thụt dòng, dùng AI CLI và model nào, AI được phép làm gì. Các công tắc đó vẫn nằm sẵn ở nơi anh dùng chúng - chip khiên trong panel AI, theme ở status bar - vì chỗ nhanh nhất để đổi là ngay chỗ anh nhận ra cần đổi. Thay đổi có hiệu lực ngay.",
    keywords: "cài đặt tuỳ chọn cỡ chữ xuống dòng theme ngôn ngữ ctrl+,",
  },
  {
    id: "ai-composer",
    section: "AI",
    title: "Chỉ đúng thứ cần hỏi cho AI",
    body: "Gõ @ trong khung chat là hiện bảng chọn file ngay trên đó - gõ vài chữ trong tên là đủ, mũi tên và Enter để chọn, đường dẫn tự vào tin nhắn. Lý do có nó đúng một câu: CLI cần đường dẫn, mà không ai nhớ đường dẫn. Khi hội thoại còn trống, các gợi ý phía trên khung được chọn theo đúng thứ đang ở trước mặt anh - file đang mở, các thay đổi chưa commit, viết test nếu dự án chạy được test, viết README nếu chưa có - nên câu hỏi đầu tiên là câu đáng hỏi. Mỗi gợi ý chỉ là một prompt: gửi rồi nói chuyện tiếp bình thường.",
    keywords: "@ nhắc file bảng chọn đường dẫn gợi ý mở đầu khung chat",
  },
  {
    id: "inline-ai",
    section: "AI",
    title: "Gợi ý ngay tại con trỏ",
    body: "Ctrl+Alt+Space bảo AI CLI của anh viết nốt dòng hoặc khối đang đứng; kết quả hiện dạng chữ xám mờ, Tab để nhận, Esc để bỏ. Nó đọc code quanh con trỏ nên gợi ý hợp với file chứ không chung chung. Chip trên status bar cho thấy nó đang làm gì và bấm một cái là đổi chế độ: Khi gọi (mặc định), Tự động - gợi ý sau khi anh ngừng gõ - hoặc Tắt. Không để Tự động làm mặc định là có chủ ý: mỗi gợi ý chạy CLI như một tiến trình riêng, đo thực tế ở đây khoảng bảy giây và cỡ một xu Mỹ mỗi lần, nên tự gợi ý mỗi lần ngừng gõ là tiêu cả thời gian lẫn tiền mà chưa hỏi anh. Bấm lại phím tắt ở đúng chỗ cũ thì miễn phí - câu trả lời đã được nhớ. Cái này khác với completion của language server ở Ctrl+Space: cái kia tức thì, chạy máy anh và miễn phí; hai bên trả lời hai câu hỏi khác nhau.",
    keywords: "gợi ý mờ ghost text inline completion ctrl+alt+space tự động hoàn thành ai copilot",
  },
  {
    id: "ai-actions",
    section: "AI",
    title: "Hỏi AI ngay trên đoạn code đang xem",
    body: "Bôi đen đoạn code, chuột phải, menu của editor có sẵn: giải thích đoạn này, viết lại cho gọn, viết test, tìm và sửa bug, thêm chú thích. Aime tự soạn prompt kèm tên file và đoạn code, nên anh không phải gõ gì cũng không cần nhớ đường dẫn. Không bôi đen thì nó lấy cả file. Việc nào có sửa file đều hoàn tác lại được bằng nút 'Hoàn tác lượt này'.",
    keywords: "giải thích refactor test sửa bug chú thích chuột phải bôi đen menu ai",
  },
  {
    id: "palette",
    section: "Giao diện & phím tắt",
    title: "Command palette",
    body: "Ctrl+K (hoặc Ctrl+P) mở một ô cho tất cả: gõ để tìm mờ cả lệnh lẫn file trong dự án; bắt đầu bằng '>' để chỉ hiện lệnh. Mũi tên di chuyển, Enter chạy lệnh hoặc mở file, Esc đóng.",
    keywords: "tìm nhanh mở file lệnh ctrl+k ctrl+p fuzzy",
  },
  {
    id: "panels",
    section: "Giao diện & phím tắt",
    title: "Các panel",
    body: "Kéo vạch chia để đổi kích thước. Kéo nhỏ quá mức tối thiểu thì panel thu thành thanh mỏng kèm nút mở lại. Ctrl+B (cây file), Ctrl+L (AI), Ctrl+` (terminal) hoặc nút trên status bar để bật/tắt.",
    keywords: "bố cục sidebar thu gọn",
  },
  {
    id: "theme-lang",
    section: "Giao diện & phím tắt",
    title: "Theme & ngôn ngữ",
    body: "Trên status bar: nút mặt trời/mặt trăng đổi sáng/tối, nút EN/VI đổi ngôn ngữ giao diện.",
    keywords: "dark light tối sáng tiếng việt",
  },
  {
    id: "shortcuts",
    section: "Giao diện & phím tắt",
    title: "Toàn bộ phím tắt",
    body: "Ctrl+K/Ctrl+P command palette · Ctrl+S lưu · Ctrl+B cây file · Ctrl+L panel AI · Ctrl+` terminal · Ctrl+Shift+N cửa sổ mới · F1 trợ giúp này.",
    keywords: "keyboard phím tắt",
  },
];

export const HELP_TOPICS: Record<Locale, HelpTopic[]> = { en: EN, vi: VI };

/** Case-insensitive filter over title, body, and keywords. */
export function searchHelp(topics: HelpTopic[], query: string): HelpTopic[] {
  const needle = query.trim().toLowerCase();
  if (!needle) return topics;
  return topics.filter((topic) =>
    `${topic.title} ${topic.body} ${topic.keywords ?? ""} ${topic.section}`.toLowerCase().includes(needle),
  );
}
