[package]
name = "ai-mini-editor"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "ai_mini_editor_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[lints.clippy]
all = { level = "warn", priority = -1 }
unwrap_used = "warn"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
tauri-plugin-clipboard-manager = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["process", "io-util", "sync", "macros", "net", "time", "rt"] }
notify = "8.2.0"
notify-debouncer-mini = "0.7.0"
dunce = "1.0.5"
portable-pty = "0.9.0"
tauri-plugin-updater = "2.10.1"
tauri-plugin-process = "2.3.1"
# Work trackers (trackers/) talk HTTP from here, not from the webview: their
# APIs send no CORS headers, and a token the frontend never receives cannot
# leak from it.
#
# The feature list is the updater plugin's, deliberately: that plugin already
# builds this exact reqwest (rustls without a bundled provider) plus rustls on
# `ring`, so Aime links one TLS stack instead of two. Taking reqwest's defaults
# instead pulled in a second provider (aws-lc-rs), which needs cmake and a C
# compiler to build - a price nobody cloning this repo should pay for six REST
# calls. The provider is installed at first use (trackers/http.rs).
reqwest = { version = "0.13", default-features = false, features = [
  "json",
  "rustls-no-provider",
  "system-proxy",
] }
rustls = { version = "0.23", default-features = false, features = ["ring"] }
async-trait = "0.1"

