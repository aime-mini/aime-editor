# Tag a version, get installers for all three systems plus the signed
# `latest.json` the in-app updater reads.
#
# The release is created as a draft on purpose: a human reads the notes and
# presses publish, and only then do existing installs see the update.
#
# Required repository secrets:
#   TAURI_SIGNING_PRIVATE_KEY           the updater private key (keep a backup!)
#   TAURI_SIGNING_PRIVATE_KEY_PASSWORD  its password, empty string if none
#
# Optional, and only worth adding once the certificates exist. Without them the
# installers still work; the operating system just warns before the first run:
#   WINDOWS_CERTIFICATE / WINDOWS_CERTIFICATE_PASSWORD   Authenticode .pfx, base64
#   APPLE_CERTIFICATE / APPLE_CERTIFICATE_PASSWORD       Developer ID .p12, base64
#   APPLE_SIGNING_IDENTITY, APPLE_ID, APPLE_PASSWORD, APPLE_TEAM_ID  for notarizing
#
# Two channels, distinguished by the tag:
#   v1.2.3        -> a normal release, and the manifest every install reads
#   v1.2.3-beta.1 -> a pre-release published under the moving `beta` tag, which
#                    only installs set to the beta channel ever look at
name: release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

env:
  # A tag like v0.2.0-beta.1 is a pre-release; v0.2.0 is not.
  IS_BETA: ${{ contains(github.ref_name, '-beta') }}

jobs:
  build:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            args: ""
          - platform: macos-latest
            # One bundle for both Apple architectures.
            args: "--target universal-apple-darwin"
          - platform: ubuntu-22.04
            args: ""

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install Tauri's Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - run: npm ci

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # Code signing by the operating systems' own schemes. Absent secrets
          # mean unsigned installers, which is a warning on first run, not a
          # failure - so a release never waits on a certificate purchase.
          WINDOWS_CERTIFICATE: ${{ secrets.WINDOWS_CERTIFICATE }}
          WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          # A beta lands on the moving `beta` tag, so the stable channel's
          # "latest release" never becomes a pre-release by accident.
          tagName: ${{ env.IS_BETA == 'true' && 'beta' || github.ref_name }}
          releaseName: "Aime ${{ github.ref_name }}"
          releaseBody: "See the assets below to download and install."
          releaseDraft: true
          prerelease: ${{ env.IS_BETA == 'true' }}
          # The updater reads this file from the latest release.
          includeUpdaterJson: true
          args: ${{ matrix.args }}
